"""Unit tests: one module, function or class, with dependencies substituted.

Fast, deterministic, and blind to the world. No network, no external service, no
real credential, no wall-clock dependence and no reliance on execution order.

A unit test that needs a live exchange, a real clock or a credential is not a
unit test with an awkward setup — it is evidence the code under test is built
wrong (``ENGINEERING_CONTRACT.md`` invariant 11).
"""
