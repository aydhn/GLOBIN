# Testing Strategy

## Why testing carries unusual weight here

GLOBIN uses a master-only workflow (ADR-0005), so there is no pull request and
no reviewer standing between a change and the repository. The test suite *is*
the gate.

It also has a second job. Most contributors to this project are agents with no
memory of previous sessions. Prose can be misread or skipped; a failing test
cannot. So wherever a project rule can be expressed executably, it is — this is
principle 10 in [`ARCHITECTURE_PRINCIPLES.md`](ARCHITECTURE_PRINCIPLES.md).

## Test levels

Six levels, one directory each under `tests/`. A test's directory decides its
level; there is no second place to declare it.

| Level | Directory | Scope | Speed | Network |
|---|---|---|---|---|
| **Smoke** | `tests/smoke/` | The smallest set of checks that would catch a broken tree | Instant | Never |
| **Contract** | `tests/contract/` | Project invariants: identity, policy, documentation, packaging, quality configuration | Instant | Never |
| **Architecture** | `tests/architecture/` | The layer contract checked against the real import graph | Instant | Never |
| **Unit** | `tests/unit/` | One module, function or class, dependencies substituted | Fast | Never |
| **Property** | `tests/property/` | An invariant asserted over generated input rather than fixed examples | Fast | Never |
| **Integration** | `tests/integration/` | Several GLOBIN components together, still entirely local | Moderate | Never |
| **External** | Does not exist yet | Real Binance non-production endpoints | Slow | Yes, explicitly opted into |

**No test at any level that exists today may touch the network.** Since Phase 005
that is enforced rather than requested: an autouse fixture refuses outbound
connections, and the section on [isolation](#isolation-and-the-offline-guarantee)
describes how. External tests arrive with the API layer (Phases 033-048), will
carry the `external` marker, are deselected by default, and must never run
against production or with live credentials.

### Choosing a level

The distinctions that are easy to get wrong:

- **Unit versus integration** is about *collaborators*, not size. If the
  dependencies are substituted, it is a unit test however many lines it takes.
  If real collaborators are wired together — normally through the composition
  root — it is integration.
- **Integration versus external** is about *the network*. Several components in
  one process is integration. Another system on the far side of a socket is
  external, and does not exist in the repository yet.
- **Contract versus everything else** is about *what is asserted*. A contract
  test asserts a project rule — a policy, a layout, a configuration — rather
  than the behaviour of code. It is the level that makes a written rule
  enforceable.
- **Architecture** is contract-level in spirit, separated because it is the one
  body of checks that reads the source tree as data.
- **Smoke** is not a lighter unit test. Ask whether it would fail for a change
  that makes the repository unusable. If it would only fail for a subtle logic
  error, it belongs at a later level.
- **Property versus unit** is about *whether an invariant exists*. If the claim
  is "for every input in this space, X holds", it is a property test. If the
  claim is "given this input, the answer is that", it is a unit test, and
  dressing it up with a two-element strategy makes it slower without making it
  stronger. See [property-based testing](#property-based-testing) below.

### Markers

Every test carries exactly one **level** marker, applied automatically from its
directory by a collection hook in `tests/conftest.py`. Deriving it means
`pytest -m unit` cannot disagree with the layout, and a moved file cannot keep a
stale label.

Five **attribute** markers are registered and applied by hand. They describe a
property of a test rather than its level, so a test may carry none or several:

| Marker | Meaning |
|---|---|
| `slow` | Worth deselecting during a tight edit loop |
| `network` | Requires network access; never permitted below the external level |
| `external` | Talks to a real external system; deselected by default |
| `windows` | Depends on behaviour specific to the Windows host |
| `loopback` | Connects to a listener this process started; nothing else is permitted |

`network` and `external` do a second job since Phase 005: either one exempts a
test from the offline guard. Nothing carries them today, and the exemption is
written now so that the guard has a documented door rather than acquiring an
undocumented one later.

`external` also became true in Phase 005. Its description had promised tests
were "skipped by default" since Phase 004 while nothing deselected them — the
selection is now composed into each expression in the command table. It is not in
`addopts`, deliberately: a command-line `-m unit` overrides an `addopts` `-m`, so
the exclusion would have silently vanished from exactly the selective runs.

Markers are registered in `pyproject.toml`, and `strict_markers` makes an
unregistered one a collection error rather than a warning.

That distinction was learned the hard way and is worth stating: the flag
`--strict-markers` placed in `addopts` does **not** take effect — pytest emits
`PytestUnknownMarkWarning` and the run passes. Only the ini option is enforced.
The repository carried the ineffective form from Phase 001 until Phase 004
tested it, which is why `tests/contract/test_quality_contract.py` now proves the
behaviour against a throwaway project rather than asserting that a string
appears in a list.

Select a level, or exclude an attribute:

```bash
python -m pytest -m unit
```

```bash
python -m pytest -m "not slow"
```

## Fixtures

Fixtures are declared at the narrowest scope that serves them.

| Where | Holds |
|---|---|
| `tests/conftest.py` | Only genuinely repository-wide fixtures, inherited by every level |
| `tests/<level>/conftest.py` | Fixtures shared across one level, when one is needed |
| The test module itself | Anything used by a single module |

A root `conftest.py` applies to every test in the suite, so everything added
there is paid for by the whole suite. It is the most convenient place to put a
fixture and usually the wrong one.

Two rules keep this workable:

- **Fixtures go in `conftest.py`; importable helpers go in `tests/support.py`.**
  A `conftest.py` is loaded by pytest rather than imported by name, so
  `from conftest import ...` only ever worked because pytest happened to place
  `tests/` on `sys.path`. Helpers a test imports live in a real module.
- **Session scope for immutable repository state.** The fixtures that read
  `ROADMAP.md` or ask Git which files are committable are `scope="session"`:
  re-reading per test would be slower and could not produce a different answer.
  A fixture whose value a test may mutate must not be session-scoped.

### Autouse

**Two autouse fixtures exist, and adding a third needs an argument.** An autouse
fixture runs for every test in the suite, and unlike an ordinary one it is
invisible at the point of use: a test that depends on it does not say so. The bar
is therefore not "is this useful for many tests" but "would this be worthless
anywhere narrower".

Both current fixtures clear it because they are guarantees, and a guarantee that
holds for most of the suite is not a guarantee. They are described under
[isolation](#isolation-and-the-offline-guarantee).

Anything that merely saves typing is not autouse. Request it by name, so that a
reader of the test can see what it depends on.

## Test data and factories

Later phases will need a great deal of Binance-shaped data, and the habits that
make that bearable are cheaper to establish now than to retrofit. Four rules.

**Build data with a factory, not a literal repeated in twenty tests.** A factory
supplies safe defaults and lets a test override only the field it is actually
about, so the test reads as the one thing that distinguishes it. `layer_policy`
and `architecture_contract` in [`../tests/support.py`](../tests/support.py) are
the worked examples: both default to the strictest possible value, so a test that
permits something has to say so, and a reader learns what the test is about from
the arguments it passes.

Factories are plain functions with keyword arguments. No builder classes, no
inheritance hierarchy, no registry — a factory that needs its own documentation
has become a second system to understand before reading a test.

**Fixture data is small, synthetic and deterministic.** Timestamps are fixed
values, never `now()`. Numeric edge cases — zero, negative, the boundary either
side of a limit — are written out rather than left to be inferred. Nothing is a
captured production payload: a real response is large, mostly irrelevant, and
carries whatever happened to be true on the day it was captured.

**No fixture contains anything credential-shaped**, including a realistic-looking
fake. `test_repository_contract.py` rejects credential-shaped filenames, but a
convincing key inside a document is exactly the thing a future reader copies. Use
an obviously synthetic value such as `SENTINEL-VALUE-4f2a`, which is also what
makes finding it in output unambiguous.

**A document written to be invalid is written at run time, into `tmp_path`.**
`.pre-commit-config.yaml` runs `check-toml` and `check-yaml` over every file in
the tree, so a committed malformed fixture fails the hygiene gate rather than the
test it was written for. `test_architecture_contract.py` and
`test_configuration.py` both write theirs inline.

## Isolation and the offline guarantee

Two rules in this document used to be things a contributor had to remember.
Phase 005 made them fixtures, because both fail in a way that does not produce a
failing test: reaching the network passes on the machine that wrote the test and
fails elsewhere, and leaking process state produces a failure in a *different*
test that did nothing wrong.

**Nothing may open a socket.** `socket.socket.connect`, `socket.socket.connect_ex`
and `socket.create_connection` are replaced for the duration of every test, and
the replacement calls `pytest.fail`. It is deliberately not an `OSError`: a
realistic connection error is what retry code is written to swallow, so once such
code exists it would absorb the guard and the suite would go on reporting itself
offline while doing nothing of the kind. A test marked `external` or `network`
keeps the real socket.

**`bind` and `listen` were never blocked, and a third marker narrows the door
rather than widening it.** The guard defends against reaching *out*, so a test
that opens a listening socket on this machine reaches nothing and was always
permitted. What it could not do was connect to its own listener, which made an
end-to-end test of Phase 027's loopback HTTP surface impossible. A test marked
`loopback` may connect to a loopback address and to nothing else: the host is
parsed rather than compared, and a marked test aiming at a real service still
fails. `network` would have removed the guarantee entirely, and its own
registration forbids it below the external level.

Name resolution is not blocked. Nothing can act on a resolved address without
then connecting, and blocking DNS would only replace a specific message with a
vaguer one. Subprocesses are not covered either — the patch applies to this
interpreter.

**Nothing may leave the process altered.** The environment and the working
directory are captured before each test and compared afterwards; a test that
moved either is failed *and* the state is put back. Both halves matter. Restoring
alone would keep the suite green while the leak stayed; failing alone would name
the culprit and then let the damage reach every test that follows.

Use `monkeypatch.setenv` and `monkeypatch.chdir` to change either deliberately.
They undo themselves, and the guard is the net beneath them rather than a
substitute for them.

The detection itself is a plain function in `tests/support.py` rather than logic
buried in the fixture, so that it can be given its own failing cases — a checker
running in teardown is the easiest place in a suite for a silent failure to hide.
The reasoning behind all of this, including why the network guard must not use
`monkeypatch`, is in
[ADR-0024](adr/0024-tests-are-offline-and-isolated-by-construction.md).

## Naming

| Kind | Convention |
|---|---|
| Test module | `test_<subject>.py`, in the directory for its level |
| Contract module | `test_<subject>_contract.py` |
| Test function | `test_<the rule being asserted>` — a sentence, not a label |
| Test double | A private class, `_FixedContract`; never `Mock` or `MagicMock` |

Function names are long on purpose. A failing line in CI output should already
be the diagnosis: `test_domain_imports_no_outer_layer` says what broke, where
`test_imports_2` requires someone to open the file.

### Test doubles

**A hand-written class satisfying a `Protocol` structurally is the default.**
That is what proves a port is a real seam rather than decoration — a mock would
satisfy any interface, including one the production code does not have.

**Where a mock is genuinely the right tool, it must be specified.** Use
`create_autospec(target, spec_set=True)`, never a bare `Mock()` or
`MagicMock()`. The autospec checks call signatures against the real object, so a
test fails when the thing it stands in for changes shape; `spec_set` additionally
refuses attributes the real object does not have, so a typo in an assertion fails
instead of silently creating another mock. An unspecified mock accepts every call
and every attribute, which means it goes on passing after the code it doubles has
gone.

The one place this applies today is the double for `tools.quality.runner.run` in
`tests/unit/test_quality_runner.py`. It replaced a hand-written stub, which had
encoded the runner's signature a second time and would have kept accepting the
old one after the real signature changed.

**Patch where a name is used, not where it is defined.** `main` binds `run` at
import, so patching `tools.quality.runner.run` would leave the bound reference
untouched; the patch goes at `tools.quality.__main__.run`.

**Prefer a real object when one is cheap.** A frozen dataclass built from
literals is clearer than a mock of it, and it cannot drift from the type it
represents.

## What the suite enforces, and why

| File | Enforces |
|---|---|
| `test_project_contract.py` | Identity is GLOBIN/`globin`; branch is `master`; 320 phases; Binance Global is the only venue; paid runtime services and scraping are prohibited; the contract object is immutable; no trading surface is exposed |
| `test_roadmap_contract.py` | Twenty contiguous 16-phase bands matching the charter; every phase 001-320 present exactly once in ascending order with a unique title and real purpose; no future phase marked complete; every artefact that states the delivered frontier in prose — `README.md`, the roadmap banner and the package docstring — agrees with it |
| `test_documentation_contract.py` | Required documents exist, are substantive, open with a heading, state the policies they own, carry no placeholder debt; ADRs are contiguous, well-formed, indexed, carry a known status and a consistent supersession record, and the README's count of them is right; the research ledger is properly structured; no branch instruction contradicts master-only; the README's maturity table claims only known states, still admits that everything else is unbuilt, links every implemented capability to what proves it, and its list of absent capabilities is still true of the package |
| `test_repository_contract.py` | The engineering contracts exist and are committable; every repository-relative Markdown link resolves; change templates ask the right questions; no credential-shaped file would be committed; tool configuration is not duplicated outside `pyproject.toml` |
| `test_packaging_contract.py` | Distribution name matches the package; **runtime dependencies are empty**; the interpreter floor is evidence-based; version is single-sourced; no licence is invented; `CONTRIBUTING.md` names every development tool and counts them correctly |
| `test_architecture_contract.py` | The declared layers exist; no import crosses a boundary outward; the inner layers reach no I/O-capable module; importing a layer performs no work; there is no import cycle; the shared policy modules import no layer |
| `test_architecture_properties.py` | Over generated module names and import graphs: a module belongs to a layer only on a dotted boundary; a path and its dotted name round-trip; a relative import is refused at every depth; a cycle is reported the same way whatever member it is found from; a review does not depend on the order modules are supplied in |
| `test_roadmap_properties.py` | Over generated phase numbers: every phase in 1..320 resolves to exactly one band and every number outside it is refused; band membership agrees with the declared bounds |
| `test_quality_contract.py` | Every test module sits in a taxonomy directory and every level holds real tests; markers are registered, applied and documented; an unregistered marker is genuinely rejected; coverage is branch-aware and gated; the CI workflow is least-privilege, SHA-pinned, secretless and unable to fail quietly; hook and CI tool versions agree; `QUALITY_GATES.md` lists every quality command in order and this table describes every test module |
| `test_quality_runner.py` | A failing step's exit code is propagated unchanged; execution stops at the first failure; a missing tool fails distinctly and is never installed automatically; no verification command modifies the tree; `python -m tools.quality` works as a process |
| `test_error_taxonomy_contract.py` | Every fault descends from one root; the categories and the fault domains correspond exactly and neither may grow alone; no category inherits a builtin error type; the root declares no domain |
| `test_isolation_contract.py` | A connection attempt is refused and a `network`-marked test is not; the drift detector reports added, removed, altered, moved and deleted-directory cases, and stays quiet otherwise |
| `test_import_surface.py` | The package and every layer import cleanly; the declared public surface exists; no trading surface has appeared |
| `test_architecture_review_end_to_end.py` | The composition root wires a review over the real package rather than an empty directory, and that review is clean |
| `test_observability_contract.py` | The redacted-name list and the severity levels in `LOGGING_POLICY.md` and in the code are the same, in both directions; the documented record shape is the emitted one |
| `test_observability.py` | A sensitive name is recognised however it is written; an event redacts itself even when built directly; binding returns a new logger; a value JSON cannot represent is rendered rather than refused; a failed write propagates |
| `test_observability_properties.py` | Over generated input: a value survives exactly when its name is not sensitive, at any nesting depth; redaction is idempotent; no field value can make the sink emit something that is not JSON |
| `test_logging_end_to_end.py` | The wired logger writes parseable records that share a correlation id, and a planted credential does not reach the stream |
| `test_configuration_contract.py` | The settings register in `CONFIGURATION_POLICY.md` and the model declare the same settings, in both directions; each documented default, written back through the binding, resolves to the value the model would have used anyway |
| `test_configuration.py` | A layer refuses an empty origin and a repeated key, and orders itself; the fold never refuses; the key register and the defaults are derived from the dataclass; an unknown key, a misspelled severity and a numeric one are each refused by name; a quoted TOML key containing a dot is refused rather than collided |
| `test_configuration_properties.py` | Over generated layers: resolving never raises; no key is lost or invented; the strongest layer wins whatever came before; silence preserves; an empty layer is the identity; a threshold forwards a record exactly when the record clears it |
| `test_diagnostics_endpoint_end_to_end.py` | Over a **real loopback socket**, and deliberately only what a socket can prove: the surface reachable on `127.0.0.1` and nothing bound until `start` is called; each route answering with the schema it actually declares, so `/health/runtime` publishes a Phase 024 health snapshot under its own name rather than relabelled; readiness 503 until the gate opens and 503 again once a stop is requested, which is the ordering a supervisor depends on; a `HEAD` reporting the length a `GET` would and sending no body; both exposition formats negotiated over the wire with the terminator OpenMetrics requires; every state-changing verb answered **405 with `Allow`** rather than the library's 501 HTML page, which is the hole `send_error` had to be overridden to close; a body, a framed body and an unreadable request line all refused without an HTML page; no response header split by anything a client sent; the hardening headers and the **absent `Server` header** read off the wire rather than off the code; a canary pushed in through the target, the query and a header value and found in no body, header or record; the pool exactly as large as its bound with **no thread per connection** (twenty requests through two workers, thread count unmoved); capacity exhaustion answered 503 on the accept loop without a new thread; and the port released, every worker joined, `start` and `stop` idempotent, and a restart working |
| `test_configuration_end_to_end.py` | A severity written in a document actually stops a record being written; a caller configuring nothing sees Phase 006's behaviour unchanged; an unknown setting is refused wherever it sits in the source order, and a value a stronger layer replaces is deliberately not validated |
| `test_config_layout.py` | The configuration layout as a *kind* rather than a register: four roles each computing one POSIX, project-relative spelling; a layout refused at construction if any segment could leave its directory, reusing the runtime tree's own `segment_problems` rather than a second copy; documents returned as a **mapping**, because a tuple has an order and a reader would read that order as a precedence Phase 027 has not chosen; and a profile name refused for every way one reaches a filename and should not — uppercase, which a case-insensitive filesystem would collapse onto another profile, a space, a traversal, an included suffix. Resolution forgives case and surrounding whitespace, because both are accidents of a launcher argument's transport, and forgives **nothing else**: an undeclared name is refused rather than matched to a neighbour, which is ADR-0006's "never downgraded" rule read at the level of a filename |
| `test_configuration_layout_contract.py` | The register, held against the roadmap and against the tree in both directions: the four profiles the composition root declares compared against `ROADMAP.md` row 026's own purpose text, every declared profile having a committed document where the layout says it would be and every committed document being declared, and the default proven to be one of them and proven **not** to be `live`. Plus the property that is the deliverable rather than a shortfall — every committed document folded over the declared defaults produces **exactly** the declared defaults, so "a profile changes nothing yet" is a decision somebody has to unmake rather than an oversight. The credential check reads the **parsed** document rather than its characters; a first version compared the text and failed on the base document's own header comment warning that no secret belongs there |
| `test_telemetry.py` | The pure telemetry vocabulary, where every refusal is testable with no clock and no provider: a metric name refused for each way it stops being comparable across exporters — a foreign namespace, an uppercase segment, an empty segment, a leading digit, a doubled underscore; the unit registry proved total against its enum and a duration proved stored in **nanoseconds**, which is what removes arithmetic from the recording path; the ceiling proved to be `2**53 - 1`, which is not Python's limit but every other JSON reader's; a dimension refused at declaration when its key names a credential or an unbounded value, and refused again when it declares no bounded value set; and screening proved to return **codes rather than sentences**, with a planted secret asserted absent from every problem it produces |
| `test_metrics.py` | Descriptors, the three folds and the snapshot, all from literals with no clock and no store: a descriptor refused when its **suffix contradicts its kind** — a gauge called `total` reads as a counter to every dashboard that sees it, which is wrong rather than broken — or when its unit is not carried in its name, or when a non-histogram declares buckets; **a descriptor whose declared series count exceeds its own budget refused at construction**, which is what turns bounded cardinality from a hope into arithmetic; buckets proved upper-inclusive with a repeated boundary refused because it makes a bucket that can never fill; a counter refusing a negative increment, a `bool` (since `isinstance(True, int)`) and the ceiling; a gauge refusing to fall below its unit rather than clamping, because a clamp hides an in-flight counter leaking in reverse; a point refused for every way its fields can contradict its kind; and a snapshot refusing an unregistered family, families out of registry order and points out of series-key order — order enforced rather than repaired, so a digest means something |
| `test_tracing.py` | Spans as values: an all-zero identifier refused **explicitly**, because it is exactly what a half-written propagator emits — a value that looks like an identifier and joins every trace to every other, which is why W3C reserves it; a span refused as its own parent, a root carrying a parent, a nested span carrying none, and nesting past the depth bound, which is the trace equivalent of unbounded cardinality; an operation name built from a value (`GET /orders/17`) refused for the same reason an unbounded metric attribute is; the duration proved **delegated** to `MonotonicReading.since` rather than computed, so a backwards reading is refused by the type that owns that refusal; a fault recorded as the exception **type** and a message-shaped fault refused, since a message may carry a path or a credential and a span travels; and a batch refusing a repeated span id — the strongest guarantee against a double close available without mutable state, since an `OpenSpan` is a value and completing it twice corrupts nothing, but a *published* batch holding both is a lie about how many things happened |
| `test_span_scope.py` | Context propagation, **with no event loop anywhere in the file** — on Windows the default loop builds its self-pipe from `socket.socketpair()`, whose fallback calls `connect()`, which the offline guard turns into a failure, and the `network` marker is declared unusable below the external level. The mechanism a task actually uses is `contextvars.copy_context()`, which `asyncio.Task.__init__` calls, so that is what is tested: a copied context proved to see the span open when it was copied, a child's `set` proved not to write back, two copies proved isolated, and the **documented trap** — a context copied *before* a span never sees it — asserted so it is a known limit rather than a surprise. Plus the token restored on both the normal and the raising path, nesting past the bound yielding nothing while the block still runs, an exception recording the type and **never the message**, and a credential-shaped attribute costing the span rather than the work. Two tests assert ADR-0026's boundary directly: the variable holds a `SpanContext` and nothing else, and two scopes cannot see each other — which is the property a module-level variable would destroy |
| `test_telemetry_store.py` | The mutable store, with every port a hand-written double and **no clock, no thread and no lock anywhere in the file** — the property that makes its rules testable in microseconds. One object proved to satisfy both halves of the port pair, and `MetricSource` proved to declare no way to record, which is what will make the exporter reentrancy loop untypeable rather than merely avoided. Accumulation within a series, attribute order proved not to fork one, a gauge set rather than accumulated, and an empty family proved **absent** from the snapshot rather than published as a zero. Six refusal paths each dropped and counted rather than raised, a refused observation proved to create **no series at all** — fail-closed, so an unbounded value set cannot grow a table — and a planted sentinel proved never to reach a log record. Twenty identical drops counted twenty times and **announced once**, because a record per drop turns a cardinality explosion into a log explosion and the rotation policy would then discard the record that mattered; the announcement table proved bounded under two hundred distinct rejected values. The over-budget branch is forced by pre-filling a family, because no descriptor `metrics()` returns can reach it — a check whose failing case is never exercised is indistinguishable from one that cannot fire |
| `test_metrics_properties.py` | The folds over generated observation sequences: bucket counts **always summing to the count**, the total always the sum of its inputs, and folding proved independent of order, so two collectors finishing differently produce one point; bucket placement total, monotone, and correct at both the inclusive bottom and the overflow top; a counter proved monotone **and associative**, which an implementation that clamped or saturated would break while passing every single-step test; the increment screen and the fold proved to agree, so no value passes review and then raises inside the code being measured; and every generated snapshot **encoded through the real `JsonCodec`** — the one that refuses a float at any depth — which proves the integer discipline rather than restating it, plus `shape()` proved to ignore every measurement, which is exactly what a digest over it relies on |
| `test_telemetry_policy_contract.py` | `TELEMETRY_POLICY.md` and the register compared in both directions: every declared metric documented and every documented metric declared, each row's kind, unit and attribute list checked against the descriptor rather than trusted, and every unbounded-key fragment the code enforces proved present in the document — because a refusal a reader could not have predicted is worse than no refusal. Its own deferral table is held to the phase-frontier rule too, which is what guards a new policy silently escaping the hand-written list `test_documentation_contract.py` parametrizes over |
| `test_telemetry_contract.py` | Telemetry's promises to what already exists, chiefly the one that justifies its design: the two denylists proved **disjoint**, with a guard proving neither is empty and neither subsumes the other, so "reuse `is_sensitive` rather than write a second list" is honest — there *is* a second list, it answers *would this value be unbounded* where the first answers *would it be secret*, and their disjointness is asserted rather than assumed. Plus the problem vocabulary closed and uniquely spelled, each unit declaring a distinct name suffix and bounding itself at or above zero, and the attribute alphabets proved to exclude `=` and `,` — the fact the whole series-identity scheme rests on |
| `test_dependency_reader_contract.py` | The two-reader tripwire. This repository parses PEP 751 twice -- the hand-written Phase 020 gate parser, which runs on a bare CI interpreter, and `packaging.pylock`, the specification's reference implementation, which runs inside a started GLOBIN that cannot import `tools/` at all. `SOURCE_OF_TRUTH.md` permits a second copy of a fact only when a test compares the copies, and this is that test: both readers proved to agree about `lock-version` and about every name-and-version pair in **both committed locks**, with a not-vacuous guard so an agreement about nothing cannot pass. Then the divergences, each pinned as **deliberate** so it cannot silently invert: an unsupported major proved refused by both but at different *stages* -- the gate parses and reports through `version_problems`, the reference implementation refuses during parsing -- which this test discovered rather than assumed; an unknown key proved reported by the runtime and ignored by the gate; an unnormalised name proved refused outright by the reference implementation and merely reported by the gate; and the version comparison proved a **superset**, so a start-up can never refuse an environment the gate just certified |
| `test_secrets_contract.py` | `SECRET_STORE_CONTRACT.md` section 6's leak gate, which names eight surfaces and states plainly that four were uncovered when it was written: exceptions, tracebacks, standard streams and the process command line were **not covered by anything**. All four are covered here with a synthetic canary — an exception's `str`, `repr` and `args`; a traceback including a **chained cause** rendered from a nested frame, and a `__notes__` entry, both of which the same formatter renders; standard output and standard error through every printing route; and the process command line read from a **real child process**, so it is what the operating system recorded rather than what the test believes it passed. Plus the absences: no encoder in either serialization module, because `SERIALIZATION_POLICY.md` is exact or refused and for a secret the answer is refused; no module-level mutable container in any of the four modules, since a register of seen values would be the store nobody declared; and no function named for section 5's forbidden vocabulary |
| `test_telemetry_properties.py` | Over generated attribute sets: screening **total and never raising**, which is what lets a telemetry call be safe inside the code it measures; no supplied value ever reaching a problem code, asserted over generated input rather than one planted secret; problems deduplicated and in declaration order, so one diagnostic reads the same twice; attribute order never changing identity; a **series key round-tripping to its own pairs**, which is injectivity proved by reconstruction rather than by inspection; a credential-shaped key unconstructible for every spelling rather than for a list; and a floored ratio proved never to overstate, which is what makes it safe for a success rate |
| `test_environment_properties.py` | Two invariants a hand-written example can only sample. The fingerprint proved a **function** of the compatibility state and of nothing else: deterministic over arbitrary projections, insensitive to reordering and to reworded `observed`/`expected` text, and — the half that stops determinism being satisfied by a constant function — **sensitive** to any changed status. And `store_key` proved **injective**: two references differing in any part never share a key, which is what makes section 3's environment isolation real rather than a check somebody remembers to write, while the key is always lowercase, which is what the platform's silent case-folding makes a correctness requirement. Plus every snapshot record proved JSON round-trippable with `allow_nan=False`, since it reaches evidence |
| `test_diagnostics_http_properties.py` | Over generated text: negotiation **total**, because the scrape protocol has no failure mode and its own rule is that an unsupportable `Accept` falls back to PrometheusText0.0.4 — so there is no input for which an error is the honest answer; an oversized header never *partly* honoured, since parsing half a negotiation is how a parser agrees to something the client did not offer; the parser bounded in item count and weight range; every target naming a route or naming `unknown`, and only a declared path naming one that answers; and, over generated addresses in **both families**, acceptance exactly coinciding with loopback — the property a denylist of spellings could not have. This is the file that found `str.isdigit` accepting a superscript two that `int` then refuses, in two binders whose contract is that they never raise |
| `test_config_layout_properties.py` | Over generated names: a validated name can never address anything outside the configuration directory — no traversal, no absolute prefix, no backslash — for every spelling the validator accepts rather than only the four declared; the four roles always present and always distinct, since two roles sharing a document would make an unchosen precedence unobservable; the screen and the builder agreeing, so no name passes review and then raises; resolution total, returning something declared or refusing with no third outcome; and resolution idempotent, because a normalisation nobody declared is how two spellings of one profile appear |
| `test_values_contract.py` | The types, the bounds and the operation matrix in `VALUE_TYPES_POLICY.md` and the code agree in both directions; every documented outcome is produced by running the attempt rather than by comparing strings |
| `test_values.py` | Every way an amount can be inexact is refused by name — a float, a bool, `NaN`, negative zero, a float-derived decimal, an absurd magnitude; a price of zero is refused and a quantity of zero is not; comparison answers within a denomination, refuses across two and raises `TypeError` across types; quantities add and subtract exactly while a price gains no arithmetic at all |
| `test_values_properties.py` | Over generated values: every code the alphabet describes is accepted and every other refused; an amount survives being written down and read back; ordering is a total order within one denomination and always refuses across two; equality never raises for any pair of any type |
| `test_clock_contract.py` | The types, the bounds and the operation matrix in `TIME_POLICY.md` and the code agree in both directions; every documented outcome is produced by running the attempt; the document still names the phases it defers to and still answers the Phase 010 boundary |
| `test_clock.py` | Every way a moment can be wrong is refused by name — naive, a `date`, a non-UTC offset, a `tzinfo` that cannot report one, a bool where a count belongs, a value off the end of the calendar; milliseconds floor towards the past on both sides of the epoch; readings subtracted in the wrong order are refused; no test asserts that two real clock readings differ |
| `test_clock_properties.py` | Over generated instants: converting an offset never moves the point; every naive moment is refused; the millisecond projection is monotone, never moves an instant forward, and round-trips exactly from a whole millisecond; ordering is total and never refuses; equality never raises for any pair |
| `test_clock_discipline.py` | No module in the domain, ports or application layers calls an ambient clock, anywhere including inside a function; exactly one adapter reads the host clock; the dependency contract still lists `time` as I/O-capable and `datetime` as not |
| `test_precision_discipline.py` | No module under `src/globin` reads, sets or borrows the ambient decimal context, anywhere including inside a function; no module outside the domain layer reads `.amount` off a value; each guard is exercised by a case it must catch and one it must spare |
| `test_precision_contract.py` | The bounds table, the rounding-mode table and the deferral list in `PRECISION_POLICY.md` and the code agree in both directions, with each documented bound compared against the value the module actually holds |
| `test_precision.py` | Every increment refusal by name; each of the four rounding modes on both sides of its boundary, including both tie directions; an exact sum that cannot be represented is refused rather than rounded; the digit budget is rederived from the published value bounds; a hostile ambient context changes nothing and the caller's context is left untouched |
| `test_precision_properties.py` | Over generated magnitudes and grids: exact arithmetic agrees with `fractions.Fraction` or refuses; alignment is total, idempotent, bounded by one increment, direction-correct per mode and order-preserving; a hostile ambient context changes no answer |
| `test_precision_end_to_end.py` | One order-shaped calculation composes alignment, notional, a ceiling-rounded fee and a balance reduction, with every intermediate exact, every rounding named, and the whole result unchanged under a hostile ambient context |
| `test_identifier_discipline.py` | No module under `src/globin/domain` names a product, an environment or an asset as a live constant, docstrings excluded; no module there reads a source of randomness; each guard is exercised by a case it must catch and one it must spare |
| `test_identifier_contract.py` | The kinds table, the constants table and the operation matrix in `IDENTIFIER_POLICY.md` and the registry agree in both directions; each documented description is the specification's own summary rather than a second author's; every documented outcome is produced by running the attempt; the symbol form is derived from Phase 008's bounds rather than restated |
| `test_identifiers.py` | Every kind has a specification, and one without is an internal fault rather than a validation failure; each identifier accepts its canonical form and is refused by the rule it breaks for the wrong case, the wrong length, a stray character and a non-string; a fixed-length kind says `exactly` rather than `between`; two kinds carrying the same text are not equal |
| `test_identifier_properties.py` | Over generated text: the reporting predicate and every constructor agree on what each form admits; validity survives permutation; one character outside the alphabet is refused wherever it sits; minted run identifiers fit the registry's form and never repeat |
| `test_serialization_contract.py` | Every wire form has a reader as well as a writer; a monotonic reading has no encoder and the policy says why; the evidence manifest's envelope keys are the ones the domain defines and its schema name is legal under the rule this phase wrote; the envelope keys, the derived identifier column width and all four compatibility answers appear in `SERIALIZATION_POLICY.md` |
| `test_serialization.py` | Every refusal by name: a schema name outside the alphabet, at either length bound, or with an empty segment; version zero and a boolean version; a payload colliding with an envelope key; a migration that skips a version, a chain with a gap or a fork, and one spanning two schemas; a record newer than its reader; a non-finite decimal in both directions; an instant carrying microseconds; and the three `json` defaults that would each break the round trip |
| `test_serialization_properties.py` | Over generated values: every wire form read back is the value written, with a decimal compared by `compare_total` so a lost exponent fails; rendering does not depend on key insertion order; compatibility is a duality, so backward one way round is forward the other; a record ahead of its reader is refused for every version pair |
| `test_serialization_end_to_end.py` | One fill-shaped record from domain values to stored text and back through the composition root, with the stated precision reaching the bytes; the same record renders identically from separately built codecs; a stored record is migrated after it was written; a record from a newer writer is refused at the boundary; a truncated document stays a detectable fault |
| `test_endpoint_manifest.py` | The manifest's seal and the gate's command line, both reached by nothing else. All **five refusals** `load` can make — text that is not JSON, JSON that is not an object, another gate's schema, an unknown version, a document that does not match its own digest — plus an unsealed one, because a seal nobody has seen break is not a seal and the gate can never reach these paths itself (it only ever reads a document it just wrote). Then the command line called **directly** rather than only through a subprocess, which proves the wiring and measures none of the parsing: the default subcommand, five rejected spellings including an abbreviation, and the usage text asserted to state that the gate binds nothing — a gate whose usage did not say so is one somebody would hesitate to run |
| `test_endpoint_plan.py` | Every judgement the endpoint gate makes, exercised from **literals in both directions** — because a detector that never fires reports a guarantee nobody has, and a regular expression that silently matched nothing would make each check pass while comparing empty sets. A contract broken seven ways against itself (a default outside its own range, an inverted range, a budget above its own series, a series the vocabularies do not support); a route the source does not serve and, the worse direction, one the source serves that the contract does not describe; **any** address literal in the binding module including loopback, since one it can spell is one it can bind; a wildcard anywhere in the package; four numbers per bound recovered from two files, each drifted in turn; the snapshot switch defaulting *on*; a drifted content-type parameter; a vocabulary the source grew that the contract omits; and the arithmetic that earns the gate — a seventh enum member proved to break every budget that names it. Also that one enum's members are never read as another's, without which every product would be wrong in the permissive direction |
| `test_evidence.py` | The pure half of the evidence package, from literals: a JUnit report is read into counts that add up and refused when malformed; coverage totals are copied and never recomputed; a checksum manifest is sorted, self-excluding and round-trips; secret-shaped content and an absolute path are both found while a relative path and `pythonnousersite` are spared; Ruff and mypy output is read into one ordered shape and every reported path is reduced to the repository; an edited manifest fails its own digest and an unsupported schema version is refused |
| `test_evidence_contract.py` | ADR-0032's mechanically checkable conditions for the evidence gate: no dependency added in the manifest or in the imports, nothing under `src/globin` importing the tooling, absent from `fast` and `full`, writing only inside the ignored run directory, documented in `QUALITY_GATES.md`, printing only ASCII; no tool it starts can rewrite the tree and each writes its own evidence file; and a CI job that installs every tool the gate starts and uploads on `always()` without masking a failure |
| `test_endpoint_end_to_end.py` | The endpoint gate against a **real tree**, which is a different claim from the literals above: this repository proved to satisfy its own contract, all eleven checks reaching a verdict so one silently dropped from the sequence is caught, and three regressions *staged in a copy of the real tree* — a hardcoded loopback address in the binding module, a wildcard smuggled into a new module, and a seventh route added to the enum without moving the budgets that name it. Then the artefact: verified against its own digest, refused when tampered with, byte-identical across two runs, and carrying no clock, no absolute path and nothing that identifies the machine. A manifest is proved to be written even when the gate could not get as far as checking, because a gate that left no artefact is indistinguishable from one that never ran — and the gate is proved by inspection to import no socket, since the offline guard blocks outbound connections and could not see a listener |
| `test_evidence_end_to_end.py` | The gate composed with an injected process runner: a passing run writes and verifies every file and records a verdict for all five gates; a failing suite still produces evidence and still exits non-zero; coverage below the floor is recorded before it is reported; an unwritten report is unmeasured rather than passing; no published file carries this machine's repository path and the raw coverage database is not left behind; the previous run is pruned; a tampered artifact, an edited manifest, a corrupt report and a leaked secret each fail verification |
| `test_execution.py` | Collection output is parsed and checked against pytest's own count; a node ID escaped by pytest survives and a malformed one does not; the manifest digest ignores order and volatile metadata and changes when the tests or the selection do; an edited manifest is refused; the partition is complete, disjoint, balanced within one and independent of both input order and the interpreter's hash seed; only pytest's `0` and `1` are verdicts about tests |
| `test_execution_end_to_end.py` | The gate runs every shard exactly once with its own args file and its own coverage data; exit `4` and `5` are unmeasured and outrank a failure; a child that never returns is reported rather than waited for; the args files together account for every test and contain no blank line; stale files from a wider run are removed; a failure prints its seed and a replay line |
| `test_workflow.py` | Reading a CI run without a CI run: each of the four job results GitHub documents, and every other value — including a differently cased one — read as unmeasured; a required job absent from the context is unmeasured rather than omitted; a job that is not required does not become one; the declared configuration is refused when absent, empty or the wrong type; the aggregate announces its schema, records its verdict rather than leaving it derivable, is sealed against editing, renders identically twice and names no absolute path; the summary leads with the verdict, carries the artifact digest and the reproduction commands, and renders a malformed section as blanks rather than raising |
| `test_workflow_cli.py` | The entry point and the branches a passing run never reaches: no argument runs the default and an unknown or repeated word is refused; `--help` exits zero and documents all four exit codes; a bad command line exits `2`, distinct from any verdict; the module starts as a process; and an unreadable configuration, an unparseable settings file, a manifest whose gates section is not a mapping, a gate entry that is not a mapping, and a summary that cannot be written are each handled without turning into a pass |
| `test_workflow_contract.py` | ADR-0032's mechanically checkable conditions for the aggregate gate: no dependency, standard library and this repository only, nothing importing `globin`, absent from `fast` and `full`, writing only inside the ignored run directory, printing only ASCII; the declared required jobs, check name, artifact and retention each compared against the workflow in both directions; no two jobs sharing a check name; the aggregate depending on every required job and running after one of them failed; and the circular-digest guard — the aggregate is not inside the bundle whose digest it records |
| `test_workflow_end_to_end.py` | The exit-code contract against real files: everything passing is `0`, a failed required job or evidence gate is `1`, and a job that was skipped, cancelled, absent, unrecognised or unreported is `3` — as is a missing, unreadable, tampered or gate-short manifest; unmeasured outranks failed; the aggregate is written even when the run failed and leaves no partial file; the artifact digest is recorded when CI supplies one and named as missing when it does not; the step summary is appended only when GitHub asks for one; and a reader refuses an aggregate from a later schema version or another schema |
| `test_execution_contract.py` | The execution commands are reachable only through the one command table, sit between `coverage` and `mutation`, are absent from `fast` and `full`, and claim not to modify the tree; every declared requirement carries a review record and no command retries a test; the run directory is ignored by Git |
| `test_ci_security_contract.py` | What CI is trusted to do: every remote action pinned to a full commit, carrying a readable version, and agreeing with `action-pins.toml` in both directions on version and upstream, with no third-party action among them; no elevated trigger and no untrusted event field reaching a shell, while the same field passed through `env:` is spared; no over-broad scope, no missing permissions block and no masked failure; every job bounded by a timeout that matches the declared budget, covering every required job and the aggregate; the concurrency group namespaced by workflow and master runs never cancelled; the queue trigger present and no display name claimed twice; no artifact uploaded from more than one path, because a second path re-roots the archive at a least common ancestor and silently moves every file — and every one of those checkers watched failing against a deliberately broken copy held as a string, never a file |
| `test_mutation_contract.py` | The mutation configuration, its baseline and the command table name the same modules and paths; every recorded survivor is well-formed, names an operator that exists and carries an argument no placeholder could satisfy |
| `test_mutation.py` | Each operator finds its own site and no other; the excluded kinds produce none; enumeration is deterministic, uniquely identified and ordered explicitly; every mutant parses and changes one line; every pytest exit code means what pytest says, and only `0` and `1` are verdicts |
| `test_mutation_sandbox.py` | The throwaway tree is built without stale bytecode, a module can be replaced inside it without touching the real one, an exit code is reported unjudged, and a child that never returns is reported as stopped rather than waited for |
| `test_mutation_end_to_end.py` | The gate refuses to start on a configuration or baseline it cannot read; stops when the unmutated subset already fails; stops when a module replaced by `raise ImportError` still passes; and fails on a survivor the baseline does not expect, printing a block that will not pass review unread |
| `test_supply_workflows.py` | Every reference shape a workflow may carry: a full commit with a three-part version comment is accepted, while a mutable tag, a branch, a short SHA and both length boundaries either side of forty are refused; a comment that is missing, a bare major or not a version is refused; a `./` reference is not an external dependency at all; a Docker action is judged by digest rather than by commit and none is used here; a subdirectory action reports its repository so two subpaths at one commit are one dependency; exact `pip install` pins are read and ranges are not; and two jobs pinning one package differently is refused rather than resolved |
| `test_supply_inventory.py` | The three registers, and the drift between them: the real tree collects in a total order with no runtime dependency and no disagreement, and reads reproducibly; a missing or malformed manifest is refused rather than read as empty; a tool CI installs but `pyproject.toml` does not declare, a pin violating its own lower bound, and a hook revision disagreeing with the version the gate installs are each reported, while the matching cases are not; and a package URL names the ecosystem that can actually update it |
| `test_supply_sbom.py` | The two fields the specification requires; two builds of one inventory rendering byte-identically; a serial derived from the commit rather than generated, distinct per commit and per repository; a ranged dependency carrying no version and no package URL but keeping its specifier; one package in two scopes producing two distinguishable components; components ordered by the key the document itself shows; a graph stating only what is known — and the validator watched failing on a wrong specification version, a wrong format, a duplicate component, components out of order, an edge pointing at nothing, a component with no type, a missing components array and a serial that is not a URN |
| `test_supply_secrets.py` | Every pattern has a sample and finds its own shape; **no finding contains the matched text, nor a recognisable prefix of it** — the property the scanner exists to preserve, since a report that repeats a secret has published it a second time; a fingerprint is stable and short; the allowlist is per file and per pattern with a reason, exempting nothing wholesale, and no entry is stale; a file of commits, digests and seeds produces nothing, because this is not an entropy heuristic; several findings in one file are reported separately by line; and binary suffixes are not read as text |
| `test_supply_waivers.py` | A complete waiver is read and an absent register is not an error; each of the ten fields is required; a range meaning every version is refused in four spellings; an unknown schema version, a quoted date and an expiry preceding its creation are each refused; expiry is inclusive of its own date and fails the day after; and a waiver marks a finding rather than removing it, matching case-insensitively on the package and exactly on the advisory |
| `test_supply_audit.py` | The judgement without the audit: a clean result, a finding read with its fix versions, and a waiver changing only the disposition; unparseable output raising rather than reporting nothing; every failure mode — connection, timeout, retries, certificate, collection and unknown — classified as something that is not clean and saying so in words; only a completed audit counted as measured; the open count excluding waived findings while the report does not; a sorted, deduplicated requirements file; and an empty pinned set treated as a failure rather than a clean audit |
| `test_supply_capability.py` | Each response shape mapping to its own state, using the prose GitHub actually returned: a plan ceiling and a missing scope distinguished though both are `403`; an unrecognised `404` read as a failure rather than an absence; a setting read from a JSON path and compared to its expected value; a control known to be unenableable reported as unavailable rather than as a permanent failure; a body that is not JSON and a missing key each an error rather than a verdict; only a required control being off counted as a policy failure; and `UNAVAILABLE` and `NOT_PROBED` never masked as a pass |
| `test_supply_contract.py` | Phase 014's own conditions: the manifest announces its schema and version, is sealed against editing, refuses another schema or version and renders identically twice; every reason code the gate can emit is in the declared closed set; the review register and the generated inventory agree in both directions, every review carries every field, and every licence is one the policy classifies; every capability state the code can produce is explained in the policy; no module can mask a failure and none but the two named starts a process — both checked against code with docstrings and comments removed, because each forbidden string appears in the prose explaining why it is forbidden |
| `test_supply_runner.py` | The two functions that leave the process, driven by an injected runner rather than by a network: a clean child, a finding, and — the case that failed in practice — exit 1 with no payload read as a collection failure rather than as a finding count of zero; an undocumented exit code, a child that never returns and one that cannot be started, each classified as something that is not clean; output that cannot be read never guessed at; and for the probe, every control asked and recorded, a plan refusal recorded as one rather than as a failure, and a probe that could not run reported as an error rather than a pass |
| `test_supply_cli.py` | The entry point and the branches a passing run never reaches: every accepted spelling of the command line and four refusals; a bad line exiting `2`, distinct from every verdict; the usage documenting all four codes; an unwritable directory unmeasured rather than passing; the module started as a process; and the gate's own failure paths — an online run measuring both network checks, a vulnerability and a required control switched off each failing, and an unreadable tree, a malformed waiver register, a missing commit date and a tree Git cannot list each recorded as unmeasured rather than as clean |
| `test_supply_end_to_end.py` | The gate composed offline: a clean tree writes an inventory, a CycloneDX document and a sealed manifest, and returns zero; the SBOM written to disk is byte-identical to a second independent build; an unmeasured audit and an unmeasured platform probe are recorded as such and outrank a pass; the manifest names no absolute path and carries no wall-clock time; and the entry point refuses an unknown word with an exit code distinct from every verdict |
| `test_governance_plan.py` | Every judgement from literals, each checker exercised once against something correct and once against something broken: comments, blank lines and trailing comments skipped while an ownerless line is refused, because GitHub reads one as *removing* ownership; all four spellings of the catch-all recognised, since missing one would make coverage vacuous; unimplemented CODEOWNERS syntax reported rather than guessed at; each supported pattern form matching what GitHub would match, and a Windows-spelled path deliberately not matching; a sensitive path owned only by the catch-all reported as uncovered; a pattern matching nothing reported; a missing heading reported and a present one not; a template soliciting a proof of concept refused **while one warning against disclosure inside an HTML comment is not** — the failing-case guard, since a rule that flagged its own remedy would be switched off; and a declaration refused for an unknown schema, an empty sensitive-path list, a missing table, a missing key, a wrong type and text that is not TOML |
| `test_governance_manifest.py` | The fourth copy of a solved shape, asserted to be that shape: two renderings byte-identical; one ASCII line with sorted keys; the digest covering everything except itself; a manifest edited by hand refused rather than believed; a round trip; a document that is not this manifest refused by name; a different schema version refused rather than read; and every reason code reflected out of the module matching the declared closed set |
| `test_governance_cli.py` | The entry point and the branches a passing run never reaches: the command spelled or omitted, and four refusals including a flag another gate has and this one does not; a bad line exiting `2`, distinct from every verdict; the usage documenting all four codes; and the usage pointing at the gate that asks the platform, so somebody looking here for the reporting channel's state is sent to `supply` rather than concluding nothing asks |
| `test_granularity_contract.py` | The scope-amendment ledger held against `ROADMAP.md` and the decision log: the count spelled in prose equals the number of rows, every ordinal appears exactly once and in ascending order, every record exists and is indexed as a decision, each score is recomputed from its four conditions rather than trusted, only the three amendments predating the test are unscored, every displaced phase exists, every title collision still names a phase with that title, every overlap names a phase that is `Complete` and every inherited phase is still `Planned`, and the review's condition tally is recomputed from the ledger. This exists because `ROADMAP.md` says of its own amendment count "Nothing tests it" -- and it had drifted twice by the time anybody looked |
| `test_governance_contract.py` | This repository's own arrangement: every declared governing file present and committable; exactly one code-owners file, at the declared location; no owner invented, since a personal account has no teams and an unresolvable owner is ignored silently; every sensitive path and every workflow specifically owned, and every pattern reaching something real; every declared sensitive path still existing and stating why it is sensitive; the extension points deliberately absent, because creating one early settles a later phase's design question; the policy carrying its required sections; the policy and the issue chooser naming the same channel with blank issues disabled; no public template soliciting vulnerability detail; every declared capability using ADR-0045's vocabulary and none of them also probed; the reporting channel `REQUIRED` rather than recorded; and the gate registered in the one command table, absent from `fast` and `full`, declaring no module and carrying no copy of the required check's name |
| `test_governance_end_to_end.py` | The gate composed against temporary trees, with the lister injected so no child process runs: a governed tree passing and recording the argument behind each `NOT_APPLICABLE` control rather than a bare state; two invocations byte-identical; and nine separate ways the arrangement rots each caught with its own reason code — the policy deleted, a second code-owners file, only the catch-all owning a sensitive path, a pattern matching nothing, the policy losing the section naming its channel, the template no longer asking about security, a public template collecting exploits, the policy and chooser disagreeing, and a declared path that no longer exists; plus an unreadable and a missing declaration each still leaving a manifest, an ownerless line failing rather than crashing, a tree Git cannot describe reported as unmeasured rather than clean, no absolute path in the output, and the reader refusing the gate's own manifest once edited |
| `test_release_plan.py` | Every release judgement from literals, each exercised against something correct and something broken: a version read from an assignment and a source declaring none refused rather than defaulted to `unknown`; ten shapes PEP 440 permits and this project will not tag, refused by name; tag and version held to being inverses; a changelog announcing the version once accepted and one announcing it **twice** refused, which is what a careless re-run produces; a notes configuration without the `*` catch-all refused, since without it a pull request matching no category is dropped silently; a repeated identifier, one outside the declared shape, and one whose letter disagrees with its category all reported; every declared category absent from the criteria reported, because a category with no criteria is a capability group the matrix claims to cover and does not; a blocking criterion reported for `BLOCKED` and `NOT_APPLICABLE` as well as `FAIL`, since none of the three is a pass; and a declaration refused for an unknown schema, no criteria, a missing table, a status outside the four-word vocabulary, a `blocking` flag that is absent rather than defaulted, an empty reason and a wrong type |
| `test_release_manifest.py` | The sixth copy of a solved shape, plus the assets it describes: sorted ASCII keys and two builds byte-identical; the digest covering everything except itself; a manifest edited to turn its verdict into a pass refused rather than believed — the tamper case stated as the edit somebody would actually make before publishing the file as a release asset; another gate's manifest refused by name; the three-word signing vocabulary keeping annotation distinct from signature; `SHA256SUMS` sorted, lowercase and order-independent, with a differing-digest guard so a constant-returning checksum could not satisfy the ordering tests; and the checksum file refusing to be one of its own entries, since its digest would describe a file that does not yet contain the digest |
| `test_release_cli.py` | The entry point and the branches a passing run never reaches: the subcommand spelled or omitted, and eight refusals including a repeated subcommand, a flag another gate has and this one does not, and a plausible misspelling; `ready` shown to actually reach the gate, since a parser returning the right answer and a caller ignoring it would both pass every other test; a bad line exiting `2`, asserted distinct from all three verdict codes; a gate that cannot write reporting unmeasured rather than passing; and the usage documenting all four codes, pointing at `supply` for the platform questions, and explaining why there are two subcommands |
| `test_release_contract.py` | This repository's own release arrangement: the declaration parsing, every one of the sixteen categories populated, every identifier unique and matching its category, every criterion naming evidence that exists and answered by a path or a command, and **every blocking criterion passing** — the assertion the phase turns on; the prose matrix and the declaration compared in both directions on identifiers, statuses, blocking flags and requirement wording, with the headline totals and status counts parsed out of prose and compared, and the regular expression failing to match itself a failure; the declared version source being the one Hatchling reads; and the gate held to what it was added as — reachable one way, declaring no module and leaving the toolchain at seven, not imported by the package, absent from `fast`, `full` and the mutating set, writing only under the ignored root, its release branch equal to the project contract's, every reason code it can emit declared in the closed set, the verified signing state never claimed from a configuration file, and the policy still naming every published asset and keeping annotation, signature, immutability and attestation distinct |
| `test_runtime_plan.py` | Every judgement about a host from literals, and the only place most of them can be made at all: a compliant interpreter accepted and a later patch in the same line accepted with it, since the floor is a floor; a patch below the floor, a different minor line, a prerelease, a free-threaded build, a 32-bit interpreter, another implementation and another architecture each refused, and four faults at once reported as four rather than as the first; Windows 10 and 11 accepted while kernel 6.3 is refused, **a Windows Server release accepted on its kernel rather than on its year** — `"2019" > "11"` as text and as a number while being older — and an unreadable kernel reported as unmeasured rather than assumed old; `pyvenv.cfg` parsed so that a `command` holding a whole command line survives; an environment refused for a visible global site directory, a missing site-packages or version key, a wrong line, a patch below the floor, a vanished base interpreter and **a location that disagrees with the one `activate.bat` recorded**, which is how a moved environment is caught; deletion refused for a path outside the repository, a sibling inside it, a nested environment, a link, the repository root itself and five directory names that are not plain; a recorded path relative inside the tree, a fingerprint outside it, absent for `None`, stable across two calls and different for two paths; the legacy launcher's listing parsed to tags with **no path returned at all**, and the parser failing on its own warning line; and a declaration refused for an unknown schema, a missing table, a string where an integer belongs, a boolean where an integer belongs, four escaping directory names, a two-component patch and a three-component line |
| `test_runtime_manifest.py` | The seventh copy of a solved shape, asserted to be that shape: one sorted ASCII line, two builds byte-identical, the digest covering everything but itself and changing when a section does; and the four refusals that make it evidence — text that is not JSON, JSON that is not an object, a governance manifest refused **by name rather than by a missing key**, a document from another schema version refused rather than read, and a hand-edited one refused because its digest no longer describes it |
| `test_runtime_cli.py` | The entry point and the branches a passing run never reaches: the subcommand spelled or omitted, both options recognised with `bootstrap` in either order, and six refusals including a repeated subcommand, a repeated option and **an option passed to `check`**, which reads like a request to rebuild something and is refused rather than silently ignored; a bad line exiting `2`, asserted distinct from all three verdict codes; a gate that cannot write reporting unmeasured rather than passing; the usage documenting all four codes and naming the environment interpreter; and the module guard exercised as a process rather than excluded from measurement |
| `test_runtime_gate.py` | The observation and failure paths a passing run never reaches: a commit read from a detached head, a symbolic one, a reference pointing at nothing and a tree with no Git at all, each recorded rather than invented; an unmeasured finding that also found nothing still unmeasured; pip reported as absent when it cannot be located and unknown when its metadata cannot be read; **pip configuration recorded as scope and existence with no path and no value**, and `PIP_*` overrides by name with the credential in the value never appearing; the install manager's runtimes counted through the documented `--format=exe` and never named, because its JSON schema has never been read on a host that has it; a launcher that refuses, one that cannot start, and a host with no Python at all; the long-path setting read as one of three words, unmeasured when the registry refuses **and when there is no registry**; a `venv` and a `pip install` that cannot start or exit non-zero; a removal refused for a directory that is not the declared one, and the directory still there afterwards; and a report that skips a section which is not a finding |
| `test_runtime_contract.py` | This repository's own runtime baseline: the contract parsing, forbidding the global site directory, refusing a prerelease and a free-threaded build, requiring 64-bit AMD64 and Windows, its patch floor on the declared line and **within `requires-python`**, so the host the tree is verified on is never one the package says it does not support; the gate held to what it was added as — reachable one way, declaring no module, absent from `fast` and `full`, never asking for `bootstrap` from the table, writing only under the ignored root, with every reason code it can emit declared **and every declared code reachable**; the environment directory ignored and not committable; and every script held to spelling the declared directory, resolving the repository from `$PSScriptRoot`, composing no recursive delete, and changing no registry key, PATH or execution policy — with `verify.ps1` shown to use the environment interpreter and to offer **no fallback to one on PATH** |
| `test_runtime_end_to_end.py` | The gate composed against synthetic trees with every child injected, and the contract derived from the interpreter running the suite so the tests assert the gate rather than the CI matrix: a well-formed tree passing host, interpreter and environment while **reporting that it is not running from the environment it judged**, which is the one finding a temporary tree can never earn; two runs byte-identical; `check` creating nothing; **no absolute path in a manifest produced from a real filesystem**, and a contract carrying a user-directory-shaped value failing the gate rather than being written quietly; and each way a host goes wrong caught with its own reason code — an absent, site-packages-visible or moved environment, an interpreter or operating system the contract refuses, a missing, malformed or wrong-version contract still leaving a manifest, no importable pip, an empty toolchain register, a refused removal stopping the run rather than building over it, and a manifest that renders differently twice; plus bootstrap creating once and installing the workflow's own pins, **creating nothing on a second run**, refusing to build from a non-compliant interpreter, and **a contract naming `..` refused before any code reaches a recursive delete** |
| `test_runtime_properties.py` | Two invariants whose failure modes are publishing an account name and deleting the wrong directory: a recorded path inside the tree rejoining to the original as POSIX, one outside it **never containing the path, a drive letter or a home directory** for any generated input, and a directory outside the repository never a deletion target — nor any directory inside it except the declared one, nor any reparse point; plus versions surviving a round trip through their text, ordering antisymmetrically and transitively, and never ordering below themselves, which is what makes the contract's floor an accepted value rather than the first refused one |
| `test_release_end_to_end.py` | The gate composed against synthetic trees, with Git faked by writing `.git` and, where the gate genuinely starts it, a scripted runner that **runs out** rather than answering forever: a complete contract passing and publishing every asset; two runs byte-identical; `SHA256SUMS` covering the manifest and not itself while the manifest covers the assets and not itself, so every published byte is described exactly once; no absolute path in what gets attached to a public release; and every way a release goes wrong caught with its own reason code — a missing or unparseable declaration still leaving a manifest, a duplicate identifier, one misfiled against its category, an empty category, evidence that does not exist, a blocking criterion at each of the three non-passing statuses, an unreadable version, four untaggable versions, a changelog missing the version or announcing it twice, each release document deleted in turn, and a notes configuration without a catch-all; plus the preconditions asked only when requested, failing separately for a wrong branch, a dirty tree and a local branch ahead of its remote, and recorded as **unmeasured rather than clean** when Git refuses or is absent |
| `test_wheels_plan.py` | Every wheel judgement from literals, with the cases taken from filenames the index actually publishes rather than invented: a compressed tag set expanded once, a build tag not mistaken for part of the version, and four shapes that are not wheel filenames refused; `cp314-cp314`, `py3-none-win_amd64`, `py3-none-any`, `cp312-none-any` and `cp313-abi3` each recognised as installable while a later interpreter, a foreign platform, the 32-bit tag and **the free-threaded ABI** are not; the pairing asserted in both directions — `cp312-none-any` installs on 3.14 and `cp312-cp312-win_amd64` does not, which is the asymmetry a substring match cannot express; the free-threaded twin accepting `none` and `cp314t` while refusing `cp314` **and `abi3`**, since a free-threaded build offers no stable ABI; `<3.15,>=3.10` admitting the pinned line and refusing the next one, five operators refused by name rather than guessed, a bound inside the pinned line refused because a minor line cannot decide it, and an empty specifier refused because silence is not permission; an architecture with no recorded platform tag refused rather than invented; a verdict recomputed against its own evidence in both directions; a gap accepted when owned and refused when not, when owned by a delivered phase, when owned by one beyond the programme, and **when a library with no gap names an owner anyway**; and a declaration refused for an unknown schema, no libraries, a missing table, a quoted date, a non-boolean flag, a boolean where an integer belongs, an empty wheel list and a present-but-invalid `resolved_by` |
| `test_wheels_manifest.py` | The eighth copy of a solved shape, asserted to be that shape: one sorted ASCII line, two builds byte-identical whatever order the input was written in, the digest covering everything but itself and changing when any content does; and the five refusals that make it evidence — text that is not JSON, JSON that is not an object, a governance manifest refused by name, a document from another schema version refused rather than read, a hand-edited one refused because its digest no longer describes it, and one with **no digest at all** refused rather than accepted as unsigned |
| `test_wheels_cli.py` | The entry point and the branches a passing run never reaches: the subcommand spelled or omitted, and eight refusals including an abbreviation, a wrong case, a repeated subcommand and a flag spelling of a subcommand — no prefixes, because `pro` meaning `probe` is a network call nobody asked for; a bad line exiting `2`; a gate that cannot write reporting unmeasured rather than failed, since a gate that could not record its answer has not given one; and the usage documenting all four codes and stating which of the two runs reaches the network |
| `test_wheels_gate.py` | The sequencing and the failure paths a passing run never reaches, against trees each deliberately wrong in one way: a consistent survey passing and writing a manifest that records what was checked and against what; no wall clock and no absolute path in a manifest built from a real filesystem; two runs byte-identical; an absent or malformed declaration and an absent or malformed runtime contract each still leaving a manifest; a commit recorded as unknown rather than invented; a survey against another interpreter, a verdict its own evidence contradicts, a duplicated distribution and an entry scheduled by a delivered phase each caught with its own reason code; a gap failing when unowned and **passing when owned**; the free-threaded cost reported without failing; and the probe driven by an injected fetcher through agreement, a newer release, a tightened `Requires-Python`, a withdrawn wheel, an unreachable index, a page that is not JSON and JSON missing the keys it needs — the last four **unmeasured rather than passed** — plus the offline run proven to ask the index nothing, and the fetcher refusing every scheme that is not HTTPS |
| `test_wheels_contract.py` | This repository's own survey: the declaration parsing, its target matching the runtime contract, its platform tag following from its architecture, the index public and carrying no credential, and **every recorded verdict recomputed** rather than believed; every scheduled library having a wheel for the pinned interpreter, every entry naming a phase that has not shipped, every gap owned, no distribution surveyed twice, every entry naming a source and stating why it is surveyed, and the set covering every library the Phase 001 ledger names; the Binance family still capping below the next interpreter line, and **exactly one library blocking a free-threaded build**, which is what keeps ADR-0050's refusal evidenced rather than asserted; the gate held to what it was added as — reachable one way, declaring no module, absent from `fast` and `full`, never asking for `probe` from the table, writing only under the ignored root, its delivered-phase bound equal to `LAST_COMPLETED_PHASE`, every reason code it can emit declared and every declared code reachable, and nothing in the package able to write the survey it reads; and CI running both halves in the one job that already reaches the network, with the result never softened and the evidence uploaded from exactly one path |
| `test_gpu_contract.py` | This repository's own GPU contract, held against the tree it describes: the declared target matching `runtime-contract.toml`, no capability declared twice, every gap owned by a phase that can still close it, and the contract not contradicting itself by both permitting and forbidding a field; the forbidden table naming the three traps the source ledger measured, each with the reason reading it would produce; **nothing declared `required`**, which is a claim about GLOBIN rather than about the gate and stops being true in the phase that makes something depend on a device; **no observed value declared as a setting** — asserted over the parsed values rather than the file's text, because the prose above the tables names a driver version while explaining why no table may; and the gate held to what it was added as — registered once, absent from `fast` and `full`, writing only under the ignored root, every reason code declared and every declared code reachable |
| `test_wheels_end_to_end.py` | The gate composed against synthetic trees carrying one entry of every wheel shape the real survey meets: a complete survey passing offline and against an index that still agrees, with the free-threaded blocker computed over the whole set rather than asserted; **the two declarations moved apart** — a runtime contract bumped without resurveying caught as divergence, and a survey honestly resurveyed for the later line caught instead by the Binance cap, which is the evidence a move would actually need; the index moved under the record for a tightened cap, a release the survey has not seen and a wheel no longer published; an index that is down proven not to report a clean survey; and the manifest verifying against itself after every kind of run while a manifest edited to turn its verdict into a pass no longer does |
| `test_wheels_properties.py` | Three invariants whose failure modes are reporting a stack ready for a build it cannot run on, counting a foreign platform as coverage, and answering a version bound it cannot justify: over generated tag triples, deciding a well-formed wheel never raises and parsing recovers the tags the filename was built from; the free-threaded and default ABIs never substituting for each other **in either direction**, and the stable ABI never a route onto a free-threaded build; a wheel for another platform never installable and a pure wheel always installable; an ABI-bound wheel for a later interpreter never installable while an ABI-free wheel for an earlier one always is, which is the asymmetry stated as a property rather than as an example; and a range admitting exactly the lines inside it, an unsupported operator always refusing, and arbitrary text either parsing as a bound or raising, with no third outcome |
| `test_drift_plan.py` | Every drift judgement from literals, against a declaration written in the test rather than the shipped one: a policy parsing into its classes, an absent optional value reading as empty, and a declaration refused for malformed TOML, an unknown schema, a missing key naming the entry it could not name any other way, a value of the wrong type, a value outside a closed set that enumerates what is accepted, and a `class` array that is not tables; an exact key beating a prefix, the longest prefix winning whatever the file order, and a key nothing declares having no class; a flag normalising to one spelling across a JSON round trip, a nested observation flattening to dotted keys, a sequence flattening to one sorted key, and flattening independent of insertion order; **the three-way interpreter rule** — a forward patch benign, a backward patch material, a different line a violation, and an unreadable version a violation rather than benign; a benign judgement carrying no repair while a non-benign conditional keeps its declared one; an unclassified difference omitted from the judgements *and* named as undeclared, with the reverse asserted so the detector cannot pass by matching nothing; and the recompute refusing an in-place verdict with no action, an in-place verdict that writes nothing, an action recorded against a verdict that would never run it, a non-in-place verdict that writes somewhere, a benign class declaring a repair, a conditional class naming no rule, a rule named by a class that is not conditional, a key classified twice, and an implemented rule no class uses |
| `test_drift_manifest.py` | The two documents this gate writes and the fact that they can be told apart: one line of sorted ASCII JSON, a rendering independent of input key order, two builds byte-identical, a digest covering everything except itself and announcing its algorithm, and the phase recorded; a manifest refused for text that is not JSON, JSON that is not an object, another gate's schema, a version this reader does not implement, and content edited after the digest was taken; a baseline round-tripping and carrying the commit that orders it, **a baseline refused when read as a manifest and a manifest refused when read as a baseline**, and a baseline edited after it was written refused — because a half-written one would otherwise report drift that never happened |
| `test_drift_gate.py` | The gate against synthetic trees with the observation substituted: an absent policy, a malformed policy and an absent runtime contract each unmeasured and each still leaving a manifest; a policy that contradicts itself failing through the gate rather than only through the plan; **an absent baseline unmeasured rather than clean**, `check` proven not to write one, `accept` recording one and refusing to when the policy is broken, and a corrupted or hand-edited baseline unmeasured rather than treated as absent; each repair verdict earning its own reason code, a forward patch not failing the gate while a backward one does, and a change nothing classifies failing rather than passing quietly; the write boundary permitting the one thing repair does and refusing a target outside the environment, an environment outside the repository and a path that cannot be resolved; the one in-place repair rewriting one key and leaving `home` and `version` alone, adding the key when absent, reporting rather than raising when there is no file, and refused before anything is read when out of bounds; `repair` performing it and the following check coming back clean, a promised repair with no implementation refused rather than skipped, and a failed repair reported as failed; and the manifest guarded — a rendering that differs twice failing rather than being written, a baseline and a manifest each refused when carrying a home-directory path, no wall clock recorded, the subcommand recorded, two runs byte-identical, and a commit read through a symbolic head or recorded as unknown |
| `test_drift_cli.py` | Three words and what happens to a fourth: no argument meaning `check`, each subcommand recognised, an unrecognised argument refused rather than ignored, two subcommands refused rather than resolved by position, a usage error printing the usage and returning `2`, the usage text naming every subcommand and saying what an absent baseline means, that text ASCII for a Windows console, a gate that cannot write reporting unmeasured rather than raising, and the parser and the gate agreeing on the subcommand names |
| `test_drift_contract.py` | This repository's own classification: the declaration parsing, named as a machine-readable contract, **every recorded verdict recomputed**, no key classified twice, every implemented rule used, every class carrying an argument rather than a label, **exactly one class repairable in place**, and no class declaring a write outside the environment; the policy and the repairs the tool implements compared in both directions; the gate held to what it was added as — reachable one way, absent from `fast` and `full`, its summary ASCII, writing only under the ignored root, every reason code it can emit declared and every declared code reachable and prefixed, and the package committable because a `.gitignore` pattern without a leading slash matches at every depth; **the boundary asserted against the package's own source** — no `winreg`, `setx`, `Set-ExecutionPolicy`, `HKEY_`, `os.putenv` or `shutil.rmtree` — and nothing in it able to write the declaration it reads; CI accepting a baseline before comparing against it, the result never softened, and the evidence uploaded from exactly one path; and the document existing, naming every repair verdict and stating what is deferred |
| `test_drift_properties.py` | Six invariants over generated observations, whose failure modes are a gate that reports drift on a machine nobody touched and a manifest that differs between two runs of one host: **an observation never drifting from itself**, a comparison independent of the order either side was built in, differences reported in sorted key order, and — asserted as a pair so that neither a comparison reporting everything nor one reporting nothing could satisfy them — every reported difference really differing and every differing key really reported; and flattening an already-flat observation changing nothing |
| `test_drift_end_to_end.py` | The gate against this machine and these declarations, which is the only place the two can be found to disagree: the host observable, every area the policy expects reported, **every observed key classified** and no observed value carrying an absolute path; the toolchain observed for every tool the project declares and recorded at the version actually installed; observing twice reporting the same host; accepting then checking reporting no drift, which is the claim CI makes across two processes; a check with no baseline unmeasured on this repository; the manifest verifying; two accepts of an unchanged host byte-identical; a `repair` run on an undrifted host changing nothing; and the module starting and reporting its usage |
| `test_lock_plan.py` | Every lock judgement from literals, with the cases taken from what `pip lock` actually emits rather than invented: a minimal lock parsed field for field, and fourteen shapes refused rather than partly read — text that is not TOML, an absent or empty `lock-version` or `created-by`, packages that are not an array, an entry that is not a table, a package with no name, and a version, wheel list, sdist, artefact name, URL or hash table of the wrong type; the format accepted at `1.0` and refused at `1.1`, `2.0`, `0.9` and two spellings that are not `major.minor`, **a minor ahead of the reader refused because it may carry fields the gate would not check**; a producer disagreeing with the declaration, and **a locked producer whose version is not the declared one**, which is the only side of that pairing the file can answer; the target compared against the contract in five ways, and a minor line nothing can parse becoming a finding rather than an error naming another gate; a package refused for an unnormalised name, no version, no artefact, and each of `vcs`, `directory` and `archive`; one distribution locked twice; a digest missing, too short, too long, uppercase, not hexadecimal, in an algorithm the policy does not permit, and **`md5` or `sha1` refused by name whatever the policy permits**; an artefact over plain HTTP, over `file:`, from another host, **carrying a credential before the host**, given as a filesystem path, and with neither URL nor path; compatibility decided **by calling Phase 018's matcher**, with the free-threaded ABI refused because `cp314-cp314` looks like it would serve a `3.14t` interpreter and does not, and a filename whose own name or version disagrees with the entry carrying it — which is what catches a hand-edited lock; a source-only package unowned, owned by an undelivered phase, owned by a delivered one, owned beyond the programme, and **a gap recorded for a package that has a wheel**; a bound clearing, equalling and failing a `>=`, and `~=`, `==`, `!=`, `<` and an empty specifier each **refusing rather than being assumed satisfied**, which is where this module deliberately differs from the inventory; the declared roots compared against the project file **in both directions**, with the second direction the only check that sees a root the project no longer declares; every declared tool locked at a version clearing its bound; a pin and a mirrored hook revision each agreeing and each disagreeing, with the replacement named; the runtime pairing silent today, failing the moment a runtime dependency is declared without its lock, and satisfied by producing one; an environment missing a package, carrying a wrong version and carrying an unexpected extra, with a seeded distribution exempt and both sides normalised; and a declaration refused for an unknown schema, a quoted date, a value of the wrong type, empty roots, a digest algorithm whose width this module does not know, a missing table, and a gap phase that is zero, boolean or a string |
| `test_lock_manifest.py` | The ninth copy of a solved shape, asserted to be that shape: one sorted ASCII line, two builds byte-identical whatever order the input was written in, the digest covering everything but itself and changing when any section does, and the phase recorded; and six refusals that make it evidence — text that is not JSON, JSON that is not an object, **a wheels manifest refused by name** because two gates write the same shape into sibling directories, a document from another schema version refused rather than read, a hand-edited one refused, and one with no digest refused rather than accepted as unsigned; plus every reason code prefixed and upper case |
| `test_lock_cli.py` | Four words and what happens to a fifth, where two of the four reach the index and rewrite a committed file: no argument meaning `check`, each subcommand spelled out, `upgrade` taking one name and several, and **`upgrade` with no name refused rather than treated as `relock`** because promoting the narrower request to the wider one hands somebody a diff they did not ask for, over the network; nine refusals including an abbreviation — `rel` meaning `relock` is a network call and a rewritten lock — a wrong case, a flag spelling, two subcommands, a repeated one, a name given to a subcommand that takes none, and an option or an empty string where a distribution name belongs; and the usage ASCII for a Windows console, documenting all four codes, naming every subcommand, saying which two are not free, and its usage code distinct from all three verdicts |
| `test_lock_gate.py` | The sequencing, the I/O and the failure paths a passing run never reaches: a coherent tree passing and recording what it checked, **the recorded lock digest identical for a CRLF and an LF checkout of one commit** — which is the whole reason it is taken over normalised text — two runs byte-identical, and no absolute path in a manifest built from a real filesystem; an absent or malformed declaration, an absent or unparsable lock and an unreadable runtime contract each still leaving a manifest; a commit read from a detached head and a symbolic one, and recorded as unknown when there is no Git, no object name or a dangling ref; the environment comparison **unmeasured rather than passed** when the gate is not running under the project environment, and the installed-distribution reader starting no child; a `pip` that cannot start, exits non-zero, or reports success and writes nothing, each reported rather than raised; a regenerated lock kept when sound, **refused and set aside when the lock itself is wrong with the committed file untouched**, and **kept when only the registers diverge** — because refusing then would mean the pins could never be brought into line with a lock that would never exist; a regeneration that changes nothing writing nothing; an upgrade with no committed lock refused; and the constraints proven to hold the workflow pins and the producer, to drop the named package, to hold the producer even when it was named, and to let a pin override a stale locked version |
| `test_lock_contract.py` | This repository's own lock: named as PEP 751 requires and at the root, committable along with the declaration and the package — the `.gitignore`-depth trap Phase 018 fell into — and nothing under the run directory committable; the format supported, the producer declared and its locked version matching, every package reproducible and none locked twice, **every artefact carrying a digest**, served over HTTPS from the declared host, and **no URL carrying a credential**, asserted separately so the failure names that rather than a host mismatch; every package having a wheel for the pinned interpreter, none needing a source build, and the lock not empty because `pip-audit --locked` raises on one that is; the declared target matching the runtime contract, the index public, the roots agreeing with the project file in both directions, every declared tool locked at a version clearing its bound, **every pin and hook revision agreeing with the lock**, the runtime statement matching an empty `project.dependencies`, the producer recorded as experimental, and **`bootstrap` and the declaration spelling the lock the same way**; and the gate held to what it was added as — registered once, declaring no module, sitting after `drift` and before the mutating commands, absent from `fast` and `full`, writing only under the ignored root, naming its phase, every reason it can emit declared and every declared reason reachable **read from its own import list**, nothing in the package able to write the declaration it reads, and its summary ASCII |
| `test_lock_end_to_end.py` | The gate composed against synthetic trees, coherent and then pulled apart one file at a time: a coherent tree passing offline and **proven to start no child**, since `full` must work on an aeroplane; a tool added to the project without relocking, **a tool removed from the project and left in the declaration** — which nothing else can see, because pip records no dependency edges — a pin bumped without relocking with the exact replacement printed, a hook revision moved on its own, a contract moved to a line the locked wheels cannot serve, and a runtime dependency appearing without its lock, each caught with its own reason code; a removed hash, an edited artefact host, a wheel for another interpreter and a version edited in the package table but not in the filename beneath it; and the manifest verifying after every kind of run while one edited to turn its verdict into a pass no longer does, plus a regenerated lock judged against the tree before it is kept |
| `test_lock_properties.py` | Six invariants whose failure modes are silent rather than noisy — trusting an unhashed artefact and accepting a wheel built for another interpreter — so each asserts the direction that matters rather than that a good lock passes: over generated package tables, deciding a well-formed lock never raising and parsing recovering what was written; **a lock with any hash removed never passing, at every position**, because a check that looked only at the first entry would pass a lock whose last package was open; a pure `py3-none-any` wheel always serving the target and a wheel for an earlier interpreter, another platform or **the free-threaded ABI** never serving it; a version bound either deciding or refusing with no third outcome, and every operator that is not `>=` always refusing; and normalisation idempotent, since every comparison in the gate runs over normalised names |
| `test_bootstrap_plan.py` | Every bootstrap judgement from literals, so that a wrong interpreter is proven refused without owning one: a recorded path carrying its relative spelling inside the project, **only a fingerprint outside it**, and absence recorded as absence, with all six combinations the three outcomes do not describe refused at construction and an absolute spelling refused by the factory — which is what makes a fourth outcome unrepresentable rather than merely unused; the check registry's twelve identifiers in pipeline order, none repeated, each category the identifier's own first segment, and an unregistered identifier an internal error; the host judged on system and release with a release nothing can parse treated as older than any floor, the architecture on machine and pointer width and ignoring case; the implementation, the free-threaded build refused while the contract refuses it, the minor line exact **in both directions**, the patch a floor, a pre-release refused, and a version this reader cannot parse **unmeasured rather than failed**; the environment decided on the prefixes rather than on PATH, and refused for a virtual environment that is not this project's; identity read from installed metadata passing, read from the source tree **warning rather than refusing** because that is the normal state of a checkout nobody has installed, and unreadable or under another name refused; dependencies declared, locked and installed, declared without a lock, and declared but absent; configuration bound and refused; the runtime tree usable and not; a secret requirement that is empty passing **as a vacuous truth the summary names as one**; a non-passing outcome with no remediation refused, a report concluding twice about one check refused, a report missing a check not ready, a warning not stopping one being ready, **the earliest failure deciding the exit code**, unmeasured outranking failed, the aggregate refusing to answer for its own answer, and a context refused for a report that does not justify it — which is fail-closed as a property of the type rather than of a function |
| `test_bootstrap_adapters.py` | The only part that touches the world, against real trees and this real interpreter: a path recorded relative to the root, the root itself as `.`, one outside it as a fingerprint, one with no root known, and none at all; the project found from its own directory and from a nested one, **not found where no project file exists**, **not borrowed from a `pyproject.toml` naming another project**, the search surviving a malformed file it walks past, and bounded — reaching exactly as deep as it declares and no further; the runtime contract parsed field for field, and refused when absent, when not TOML, when a table or a field is missing, when a boolean stands where an integer belongs and when a string stands where a boolean does; the host and interpreter probes reading this machine, and the suite's own premise that it runs from `.venv`; identity read from installed metadata, the root recorded as `.`, and an origin that names a relative directory or nothing at all; six requirement spellings reduced to their distribution and four names normalised as PEP 503 says; what is declared, locked and installed, what is missing, what has no lock, and five project files this reader cannot use declaring nothing; the installed set read through the standard library and **starting no child**; no secret required because none is; only the allowlisted roots created, preparing twice changing nothing, a root that is a file or that **resolves outside the project** refused with nothing created, and one that cannot be created reported rather than raised; and every reason code derived from its check |
| `test_secrets.py` | The store's domain from literals, with no credential manager anywhere: a reference proved to render **in full** because it carries no material, and a name refused for each way it would become ambiguous — empty, over the bound, containing the key separator, and **uppercase**, which matters because `store_key` folds case and two spellings would otherwise become one credential silently. Then the value type's four obligations, each structural rather than advisory: redaction proved through **ten rendering routes** including `%r`, an f-string with a format spec and membership in a list, a dict and a tuple — because `object.__format__` with a non-empty spec does not route through `__str__`, so a type overriding only two would raise where a redaction must not; `vars()` proved to raise and `dataclasses.is_dataclass` proved false, which is what defeats `asdict` and every dump helper written against it; the type proved **unhashable**, removing two structures material could reach; and the ceiling exercised at 2560 and 2561, the boundary measured on the real host. Plus the key proved lowercase, distinct per environment, per kind and per slot, and a reference named `venue_key_previous` proved unable to collide with the previous slot of `venue_key` — which is why the slot is a bounded key component rather than a suffix |
| `test_secrets_adapter.py` | The Credential Manager driven by a fake `advapi32`, so a logon session with no credential set, an oversized blob and an undocumented status are all reachable from a machine where none is happening: the `CREDENTIALW` field names and order pinned, because a wrong `ctypes` offset does not raise — it reads a neighbouring field — and the blob pointer proved pointer-sized, since `c_void_p` replaced `POINTER(c_byte)` to avoid a call at import and the substitution is only safe if the widths match. A round trip, the two slots proved to be two credentials, the platform buffer proved freed **including when the decode fails**, and the persistence scope proved to be `LOCAL_MACHINE` rather than the two declined by policy. Then the fault mapping: absence, a session with no credential set, and **1783 `RPC_X_BAD_STUB_DATA`** — which `CredWriteW` documents nowhere, so anything classifying against the documented list would file the one failure the ceiling exists to cause under 'unknown'. And the encoding, which a test found rather than a review: the domain's bound and the platform's proved equal for every input, with the guard exercised by forcing the encoding back to the UTF-16 that made an ASCII secret encode to twice its length |
| `test_degradation.py` | The degraded-operation posture as a classification, with no host, no clock and no platform in the file — every combination of three necessities and five statuses constructible from literals, which is what carries the branch floor without a machine that actually lacks anything. **An opportunistic absence proved never to make a host amber**, which is Phase 030's inherited rule and the reason the third tier exists: the CI quality job installs neither psutil nor either telemetry library on any run, so the other rule would make this amber for ever. A forgiven absence proved still to appear in the document, because forgiving it in the verdict must not mean hiding it; a required component that could not be *measured* proved not to block, which is ADR-0045's rule; and the posture proved invariant under the order of observations. Then the contract reader: an absent or malformed file unmeasured, a later schema refused rather than read anyway, and each of the six real factories driven through **both** arms — the absent one reached by substituting the factory, because a host that has everything never takes it |
| `test_api_reality.py` | The registry's own refusals, one at a time, and the distinction the phase exists for asserted directly: `unknown` and `unsupported` are both unusable and are **not the same answer**, because a registry that let one imply the other would let a later phase infer support from silence. Also the four venue-named values proved to be shapes with **no list of known members** — any well-formed slug is a family, including one nobody has heard of, which is what lets the registry record a product the venue adds tomorrow. A `restricted` row refused with no condition and refused again with a condition on any other status; a FIX endpoint refused without TLS, without SNI and without a port; a trading endpoint refused as unauthenticated *and* as unknown-authenticated, since not knowing a requirement is not the same as there being none; a live host filed under a marked environment and the reverse, which is the direction that loses money; and the two FIX encodings proved independent, because one documented port takes text and answers in binary |
| `test_api_reality_adapter.py` | The reader that turns the declared document into a snapshot, and the evidence it publishes. Every narrowing's **refusal** — the branch a correct registry never reaches, and therefore the branch that would silently accept the wrong type if it broke: a string field carrying a number, an integer field carrying a string, a list field carrying a scalar, and `true` where a port belongs, which matters because `bool` is an `int` subclass and would otherwise read as port 1. Absent and wrong proved to be **different answers** — a missing file is `None` and reported unmeasured, a present broken one raises — since flattening them would let a corrupted registry report as merely unread. Then the manifest's five refusals, which the gate can never reach itself because it only ever reads a document it just wrote, and a summary proved to **count rather than copy**: one embedding the registry would change whenever the registry did and prove nothing about either |
| `test_api_reality_gate.py` | The second reader, driven entirely offline through an injected fetcher — a real fetch here would be caught by the socket guard rather than by the assertion, proving the guard rather than the gate. The allowlist attacked four ways: the approved host in the *path*, the approved host as a *suffix* of another, the right host in somebody else's repository, and a `file://` URL. That third case found a real hole — `"raw.githubusercontent.com".endswith("github.com")` is **False**, so a suffix test let any repository through. Then every finding made to fire on purpose, and the owned-unparseability rule in **both** directions: a declared-broken source is recorded rather than failed, and a declared-broken source that starts parsing **fails**, so the exemption cannot outlive its reason |
| `test_api_reality_contract.py` | The committed registry held to the rules it exists to enforce, which is a different claim from the refusals above: those build a record to make one rule fire, and these read the document that ships. Both readers proved to count the same rows, since two independent parses that disagree mean one is wrong and nobody would otherwise find out. **Nothing claims to have been observed**, because GLOBIN has never contacted the venue. FIX proved `unknown` on the three products whose documentation could not be read and `restricted` with a named condition on the one that could — the sharpest available case of a capability never being inherited. And a source proved to earn its place by supporting a claim **or** by being watchable: the first draft required every source to be cited and the changelog failed it, which was the test being wrong |
| `test_api_reality_discipline.py` | Where a venue fact may be written down. **No venue host is spelled anywhere in the package** — stronger than "only in the reader", and possible only because the registry is data rather than code. Exactly one module reaches the registry document, checked in both directions so the rule cannot pass by the reader vanishing, and both checks read *live string constants* rather than raw text after the first draft flagged its own docstrings |
| `test_api_reality_end_to_end.py` | The `api-reality` group driven through the real entry point, so parser, dispatch, reader and rendering are exercised together. Every read verb proved to answer; `--json` proved to put **only** JSON on standard output by parsing the whole stream, with the human report moved to standard error rather than discarded. An absent registry proved `unmeasured` and a *broken* one proved a failure, because flattening the two would let a corrupted document report as merely unread. And the diff proved in both directions with the direction stated, since it is easy to get backwards: an endpoint the registry no longer has is breaking and exits 1, while one it has gained is informational and exits 0 |
| `test_api_reality_properties.py` | Three invariants over generated input rather than examples: the diff is reflexive, so an empty result means agreement rather than a comparison that did not run; it is **total over all thirty-six status transitions**, since the pair that falls through unclassified is the one nobody thought of; and one moved field produces at most one finding, because a diff reporting a move twice inflates a count somebody reads as severity. Plus the band's **first opt-in network test**, marked `external` and excluded from every quality command, asking the venue itself whether all fourteen recorded sources still answer unchanged |
| `test_degradation_discipline.py` | The bidirectional tripwire that makes the component registry complete by test rather than by memory: every `reached_through` in the contract proved to name a function that exists, and **every module defining an `Unavailable*` stand-in proved to appear in the contract**. The second direction is what earns the file — a seventh absent-safe factory added without a row is a component nothing surveys, and the posture would go on saying ready about a host that had lost something. The exemption list proved to name only modules that exist, since a stale carve-out silently widens the rule it was carved from, and the checker is exercised against a module that defines a stand-in, one that defines nothing, and one that **uses** a stand-in without defining it — the last being the survey module itself |
| `test_secret_environment.py` | The hand-off provider and the router, in one file because they are two halves of one question — which mechanism holds a reference, and what that mechanism will not do. The provider proved **never to iterate its environment**, by being handed a mapping that raises on iteration and having every method exercised against it, since a scan would notice secrets GLOBIN was never handed; a write proved refused **without the material being read**, because a hand-off that could be written to would make the environment the resting place `SECURITY_BASELINE.md` forbids; and a forbidden profile proved to refuse with a policy fault rather than a platform one, which is the difference between a rule and a broken machine. Then the router's central absence: an unresolved reference proved to reach **exactly one** mechanism and the other proved to receive zero calls — asserted as a count rather than as a returned fault, because a fault can be right for the wrong reason — and a locator naming a mechanism this run does not have proved refused at construction rather than quietly defaulted |
| `test_secret_vault.py` | The vault envelope as a value, with no platform anywhere in the file: the filename proved to be a projection of the one key builder rather than a second scheme, since section 2 of the store contract says nothing else composes a key anywhere; two slots and two environments proved to be different files, because rotation needs the previous value to survive and isolation has to hold on disk as well as in a target name; and the digest proved to change with the ciphertext, with the header, and **between two protections of one secret** — which is what makes it an identifier for a file rather than the fingerprint of a value that section 5 constrains. Then the six refusals the reader performs before anything would reach the cryptography, asserted individually rather than through one 'a bad envelope fails', because a mutation that skipped the digest check would still round-trip every happy path |
| `test_secret_vault_adapter.py` | DPAPI driven by a fake `crypt32` holding ciphertext, so a corrupt envelope, an undocumented status and a platform that succeeds with nothing to return are all reachable from a machine where none is happening: the `DATA_BLOB` field names, order and widths pinned, because a wrong `ctypes` offset reads a neighbouring field rather than raising. **Machine scope proved never requested**, since an absence does not appear in a diff and that flag would make the material readable by every account on the computer; the prompt structure proved always null, which is the path that survives its February 2027 removal; and the description pointer proved null, which removes a second mandatory free rather than discharging it. The digest gate proved to run **before** the platform, asserted as zero unprotect calls, because the vendor documents that unprotection may succeed on corrupted input. And the allocation ledger: every buffer the fake hands out compared against every address the injected deallocator receives, after every path including the failing ones — a comparison a second permitted loader of `kernel32` would have made impossible |
| `test_secrets_application.py` | Requiring, resolving and rotating against an in-memory store that can be made to fail on a chosen step — the whole value of section 4's ordering is what happens when one does, and that never happens by itself. A refusal proved to name the reference and the fault and to carry neither the material nor its **length**; every fault proved to have a remediation and every remediation proved incapable of interpolating one. Then rotation, four steps and every failure between them: the previous value proved copied **before** the new one is written, because a Windows write replaces and step 3 would otherwise retire something already gone; a failed write proved to leave the previous recoverable; a read-back that disagrees proved to fail the rotation **and keep the previous copy**, since that is the one failure no compare-and-swap exists to catch; and a rotation of a reference holding nothing proved to report `previous_recoverable` as **false** rather than reassuringly true |
| `test_dependency.py` | Dependency classification from literals -- a package a Linux marker excludes, a lock resolved for an interpreter this is not, a distribution installed two patch releases from what the lock names. The state that matters most is the one that does **not** report a problem: a marked package proved `not_applicable` rather than `missing`, which is the false refusal this repository would have shipped the day it declared its first marked dependency. Versions proved compared as PEP 440 **releases** rather than as text, so `1.0` and `1.0.0` agree and nobody is sent to reinstall a correct environment. Then the fingerprint: stable across two builds, unmoved by the order observations arrive in so a relock that reorders `[[packages]]` does not look like a changed environment, moving when any of the four fields of any observation moves, and -- the one that fails the day somebody adds a volatile field -- **not** moving when the lock's own `lock-version` and unknown-key set change, because a producer bump with no package changed has changed nothing about which dependencies are here |
| `test_secrets_command_end_to_end.py` | The `globin secrets` group driven through `main` over real streams, because the parser, the composition root and the renderers are what a test calling the handlers directly would skip. **Nothing here stores a credential, and that is the point**: pytest replaces standard input, so it is not a terminal, and both writing verbs refuse before `getpass` is called at all -- the refusal that stops a key reaching shell history, exercised for real rather than asserted. Then the surface that must not exist: `--secret`, `--value`, `--material` and `--password` proved refused as unrecognised words, so a caller reaching for one gets a usage error rather than a credential in their shell history, and the canary proved absent from the error. Every part of a reference proved required and every kind outside the bounded set refused, with `SecretReference`'s own validation surfacing as usage rather than a traceback. `--json` proved refused for `set` and `rotate`, accepted for the four read-only verbs, and proved to put the document on standard output alone. Plus the usage text proved to offer no way to reveal a secret and to document exit code 25 |
| `test_materialize_cleanroom_end_to_end.py` | The clean-room harness in two layers, and the one invariant that guards both. **Layer A always runs** and builds nothing: the sequence -- create, install, probe, reconcile, remove -- is exercised end to end with an injected installer, so the room is proved to be removed on the way out without a real interpreter existing. **Layer B is opt-in**, marked `external` + `network` + `slow`, and is the only test in the repository that starts a real installer; every quality command composes `and not external`, so it runs when somebody asks and at no other time, and it reaches the index in a child process where the autouse offline guard does not apply. The assertion that matters most is neither: a decoy `.venv` beside the scratch root proved **byte-for-byte unchanged** after both a successful run and a failing one. Plus an AST scan proving the harness never evaluates a string naming `bootstrap.ps1` or `-Recreate` -- docstrings excluded, because this package explains the refusal at length and a byte scan cannot tell explanation from execution |
| `test_materialize.py` | Whether a lock could be built from local bytes, and the safety around trying. Nothing is fetched, installed or created: every failure is driven through an injected runner, so a timeout is a double that raises immediately rather than a test that sleeps. Three refusals carry the most weight. A corrupt cached artefact proved **left in place** -- deleting the evidence of a corruption is how the diagnosis is lost, and re-fetching would be the cache quietly becoming a network client. A missing artefact proved to fail rather than reach an index. And `cleanroom_problems` proved to refuse a `.venv` by four independent checks, any one of which alone suffices, including a sibling directory sharing a prefix. Then the verdict distinction that keeps a fresh clone from being red: an **empty** wheelhouse is `unmeasured` and exits 3, while a **populated but wrong** one fails -- and an artefact that is unhashed or unservable fails even with an empty wheelhouse, because fetching would not fix either. Two behaviours were measured rather than assumed and are recorded here: `Tag` is unorderable, so the declared tags are an ordered tuple because PEP 425 order *is* preference; and `Pylock.select` is all-or-nothing, raising on the first unservable package rather than yielding the rest |
| `test_entitlements.py` | What a credential may do, and what GLOBIN refuses to claim about it. The two assertions carrying the most weight are both about *not* permitting. First, the state vocabulary proved to have **no member meaning confirmed** -- pinned as a set, and separately proved to contain no member whose value holds `confirm`, `verified` or `valid`, so a future caller cannot write a branch treating an unverified declaration as a verified one. Second, a withheld grant proved unreachable **by every possible declaration**, including one that declares it, which is `SECURITY_BASELINE.md` section 4 turned from a paragraph into a branch. Then the containment: covering proved to be subset, excess proved reported rather than refused, and `require_permitted` proved to reach the store **zero times** on a refused verdict -- the observable form of the claim that no branch resolves material and then discards it. Plus the register: a round trip, a replacement rather than a duplicate, and eight malformed entries each proved dropped while its neighbour survives, because raising would take the whole register out |
| `test_environment.py` | Capability classification from literals — an ARM64 host under emulation, a Windows release predating `IsWow64Process2`, a box with no Git, none of which exists here. The two properties that carry the most weight are both about *not* failing: an unmeasurable **required** capability proved to degrade rather than block, which is what keeps a supported host startable and CI honest, and a missing optional one proved the same. Then the fingerprint: stable across two builds, moving when a status or the native architecture changes, and **not** moving when the registry is reordered, when a tool is installed, or when human-readable text is reworded. The volatile-field exclusion is asserted on the **type** — `CompatibilityProjection.__dataclass_fields__` proved to be exactly two names — because a denylist of volatile keys is a list somebody must remember to extend, and the failure when they do not is a fingerprint that changes every run. Plus the snapshot proved to have no field that could hold a path |
| `test_dependency_adapter.py` | Reading a lock, from literal documents, and the refusals are the interesting half: `read_lock` proved never to raise, because a start-up check reports a remedy rather than terminating on one. An unsupported **major** version proved refused, which PEP 751 states as a MUST, and proved distinguished from a merely invalid document -- the specific error subclasses the general one, so the order of the two `except` clauses is load-bearing rather than stylistic. An unknown key proved to warn rather than refuse, which is the specification's companion SHOULD, with the offending keys named and the packages still read. Plus the key audit proved to leave the reserved `tool` table alone, and `installed_versions` proved to report **versions** -- the thing its predecessor walked past and discarded, which is the defect the phase exists to remove. A test asserting the reference implementation's wheel-filename check was written and deleted: it passed on 26.3 and failed on 26.0, so it was asserting a library's version-specific strictness rather than any behaviour of GLOBIN's |
| `test_secret_entry.py` | Asking a person for a credential, and every way that is refused -- which is nearly the whole file, because the value of this adapter is what it declines to do. Two tests assert an **absence of a call** rather than a result: a piped stdin proved `getpass` was never reached, and an echo warning proved nothing was read, both being the difference between a value discarded and a value that never existed. `GetPassWarning` proved to abort before the operator types anything, which is stronger than `SECRET_STORE_CONTRACT.md` section 5 asks for. A `KeyboardInterrupt` proved caught rather than propagated. Material proved **refused rather than stripped** for surrounding whitespace, control characters and size. A malformed *second* entry proved to report a mismatch and not its shape. And an oversize PEM proved to trip **two** rules, which is how the file records that armoured key material cannot be collected at a single-line prompt at all |
| `test_environment_adapter.py` | Architecture detection through a fake `kernel32`, and the two traps in it. First: `IMAGE_FILE_MACHINE_UNKNOWN` is `0` and means the process is **not** emulated, so a mapping written against the constant's name would report every ordinary native host as unmeasured — and this host returns that value on every run, making the mistake permanent rather than rare. Second: `GetNativeSystemInfo` is documented to report an ARM64 host as x86 or x64, so the fallback proved to answer the **process** question only and to record the native one as `UNKNOWN` — a wrong answer being worse than none in exactly the case the capability exists to detect. Plus a failed call proved to fall back rather than raise, the modern API proved **not** called when absent and proved to make the fallback unnecessary when present, every `IMAGE_FILE_MACHINE` value mapped with an unrecognised one becoming `UNKNOWN`, and the toolchain probe proved to answer a boolean, to pass a mode including `os.X_OK`, and to survive a lookup that raises |
| `test_environment_application.py` | The mapping from what a probe reported onto the rules its neighbour tests, in its own module because the two halves measured very differently: the domain sat at 100% while this layer sat at **32%**, reached only incidentally through the bootstrap — which is exactly the weakness a repository-wide coverage floor hides. Every classification branch from literals: a matching architecture supported, a mismatched one blocking, an **unknown one degrading rather than blocking**, and the declared `AMD64` proved compared case-insensitively against the vocabulary's `amd64`, since comparing them raw would report a mismatch on a host that matches. Emulation proved `DEGRADED` and optional rather than a refusal, naming both architectures. Then the assembly: every executable proved probed **exactly once** across the whole snapshot, so the summary and the checks cannot disagree about one tool; and — the largest refusal in the phase, asserted rather than described — the snapshot proved to produce **no operating-system, interpreter or virtual-environment check at all**, because Phase 021 already judges those and two verdicts about one fact is drift |
| `test_preflight.py` | The classification and the schedule, over reports built by hand: no check is run here, because what is under test is how a report is *read*. The two durability classes proved to **partition** the registry, so a nineteenth check cannot land in neither; a `CheckSpec` built without one proved `PERISHABLE`, which is the conservative default asserted rather than described. The bounds are exercised at the floor and the ceiling **inclusively**, and `True` is refused as an interval because Python makes it an `int` and one millisecond is the accident that looks like it worked. The two verdict properties that carry the most weight are both about *not* over-claiming: a complete passing run proved to name every perishable pass, and a report built only from stable checks proved to have a shelf life of `None` rather than a large number a caller could compare against |
| `test_preflight_contract.py` | The register, in both directions: the eighteen-row durability table in `PREFLIGHT_SUITE.md` compared against `checks()` so that neither a stale row nor a missing one survives, and the spelled counts derived from the split rather than read — a `KeyError` when the split moves outside the spelling map is the intended failure, because somebody has to write the new word. Also both command groups against `USAGE`, so a verb that exists and cannot be discovered fails |
| `test_config_evidence.py` | The projection, from layers built by hand. Provenance proved to name the winning origin and to count what it overruled — a count **not recoverable** from a resolved configuration, which is why the input is layers. Then the property the whole design rests on, asserted as a pair: the evidence fingerprint proved to move when only the *source* moves and the semantic one proved not to, on the same two inputs, so the split is visible in one place. A credential-shaped value proved absent from the account, the snapshot and the drift report, and — the case a display-based comparison would get wrong — a sensitive key whose value changed proved reported as `changed`, because the comparison reads digests. The contract version is refused in both directions and for a `bool`, a string and a float that each look right |
| `test_config_cli.py` | The two surfaces Phase 030 added. Every accepted shape compared **whole** as an `Invocation` rather than field by field, and ten refusals beside them; abbreviation refused for all four options and for a subcommand, which is a property of a parser that compares words for equality rather than a flag somebody set. The three that matter most are about not over-reaching: an option not given proved **absent rather than defaulted**, since a parser default reaching the strongest layer would overwrite every source below it; `explain` proved to still answer when the model will not bind, because that operator is the one who needs the account; and a credential-shaped `--set` proved refused with its value in neither stream |
| `test_config_evidence_adapter.py` | The two artefacts, against a hand-written store rather than a real user profile. The manifest proved to declare what it is, to round-trip, and to be refused four ways -- not JSON, not an object, another schema, another version -- plus **edited**, which is the one that makes a recorded document evidence rather than a claim. Two properties are asserted over bytes rather than structures because that is where the failure would be: writing twice proved to produce identical bytes, and a credential-shaped value proved absent from the file on disk. The baseline half proves the distinction the phase rests on -- the snapshot lands in the **state** area rather than the evidence tree, carries no display, and a record written under another contract version is **refused rather than treated as absent**, because "no baseline" and "a baseline nobody can read" call for different actions |
| `test_config_evidence_end_to_end.py` | The six-layer chain assembled by the composition root, with a real document on disk at each level. Each step of the order asserted separately — an explicit document over the committed tree, the environment over it, the command line over that — and then all four at once, proving three layers overruled. The acceptance criterion the phase was set: the same explicit inputs from **two different working directories** proved to produce identical semantic *and* evidence fingerprints, and a relative `--config` proved to resolve to one absolute origin rather than two. Plus the leakage check asserted over **rendered bytes** rather than over a structure, which is where a value would actually hide |
| `test_bootstrap_application.py` | The pipeline against hand-written doubles, which is how fourteen refusals are exercised without fourteen broken machines: one step per registered check and a sentence of its own for every check that can go unmeasured; a satisfied host producing a context, the report carrying every check in the declared order, two runs agreeing, and the runtime tree prepared exactly once; **fourteen failure classes each producing its own exit code with no context on any of them** — no root, an unsupported system or architecture, a wrong implementation or version, no virtual environment or the wrong one, an unidentifiable project, a dependency unlocked or missing, a configuration that refused or named an unknown setting, an unusable tree and an unresolved reference; a gate stopping at the first refusal; an unreadable contract unmeasured rather than failed; every refusal carrying a remediation; and the diagnostic measuring everything it still can, recording the rest as unmeasured, reporting the same earliest cause, and **agreeing with the gate exactly when the host is healthy** |
| `test_bootstrap_manifest.py` | The evidence document: schema, version and phase carried so another manifest fed to this reader is refused by name; rendering canonical — one line, sorted keys, compact separators and ASCII only, so a Windows console cannot change the bytes; the digest covering everything except itself; five refusals in order for text that is not JSON, is not an object, is another schema, is another version, or **has been edited since it was written**; a refusing run recording its reason codes and no fingerprint; **a field whose name looks like a credential redacted where the record is built**; and the file written under the declared evidence root, written even when the run refused, and ending in one newline with no carriage return |
| `test_bootstrap_cli.py` | Nine command lines accepted and thirteen refused — an abbreviation, a wrong case, a flag spelling, two subcommands, a repeated option, a trailing word, an option given to `--version` or `--help`, and `--json` asked of `evidence`, which writes a file and is told what to use instead; the usage text documenting **every** exit code the process can return; the human table naming every check and ending in a sentence that says plainly whether GLOBIN may start, and the JSON being the same document the evidence carries; help succeeding to standard output, **no command a usage error rather than a success**, the version read from one source, **standard output carrying only JSON under `--json`** with the human text still written somewhere, both renderings describing one run, evidence written and its location reported, a tree that is not a checkout refused rather than guessed at, **a refusing run writing nothing outside the project**, two runs reporting the same thing, and the same project described from the repository root and from a directory inside it; plus `python -m globin` started as a real process to prove the module guard runs. Phase 032 corrected one of its assertions: `--version` is compared against `project_identity()` rather than against `globin.__version__`, because the probe prefers installed metadata and falls back to the package -- so an editable install reports the older of the two until something reinstalls it. The old assertion said the opposite and passed for eleven phases only because no phase had moved the version |
| `test_bootstrap_contract.py` | The promises a launcher branches on: **every exit code pinned to a literal number** rather than derived from the enum it checks, every check mapped to the failure class it promised, the four generic codes keeping the meanings every gate under `tools/` gives them, and no two classes sharing a number; one console script declared and delegating, the module guard proven to hold no logic, and `main` callable with no argument because the generated wrapper is; the aggregate last and named once, every identifier a lower-case dotted pair, and **no module under the package carrying a credential-shaped name**, which is `SECRET_STORE_CONTRACT.md` §1 checked rather than remembered; and the leak gate — the verifier's own scanner applied to what the product actually wrote, five sentinel values injected and asserted absent **by their own text rather than by a redaction marker appearing**, no fragment of a path from outside the project anywhere in the document, one write and it is the manifest, the gate and the diagnostic describing one host, this repository bootstrapping, and the adapter importing none of the verification tooling |
| `test_bootstrap_properties.py` | Five invariants whose failure mode is publishing somebody's account name: a path from outside the project never appearing in what is recorded for any generated input, nor any drive letter, home directory or `Program Files`; a fingerprint hexadecimal and of a fixed width, so its length says nothing about what produced it, and two different paths never colliding; **the exit code a total function of the statuses** — unmeasured outranking failed, a failure yielding the earliest failing check's own code, and everything else success, over every combination Hypothesis can build; the context fingerprint never carrying a path it was given, with the generated name drawn from letters a hexadecimal digest cannot contain so that a coincidence cannot pass for a leak; and a recorded path carrying exactly one of its two fields |
| `test_bootstrap_end_to_end.py` | The bootstrap wired by the composition root against real trees, substituting nothing: **this repository bootstrapping**, which has to be the passing case because `python.environment` compares `sys.prefix` against the environment inside the project root and a temporary checkout cannot satisfy that — stated as its own test rather than left as a footnote; only the allowlisted directories appearing and the declared-but-not-created roots staying absent; running twice leaving the tree as it was; eight contracts this host does not satisfy each refusing with its own code and no context; a tree that is not a checkout refused **before anything is read or created**; a missing or malformed contract unmeasured rather than failed; a dependency declared without a lock and one locked but absent; the same project found from every directory inside it with identical facts and verdicts; **a checkout nested inside another project not borrowing its parent**, with the evidence landing under the inner root and nowhere else; the manifest written inside the tree and reading back sealed, two runs producing identical bytes, and evidence refused where there is no project; and the command line reporting the same verdict as the pipeline, with a refused run still writing its evidence and its reason code |
| `test_stack_plan.py` | The scientific-stack judgements from literals, with `numpy` and `pandas` absent from the process entirely — which is the design as much as the code, since proving a wrong `float64` is refused would otherwise need a wrong `float64`: each probe expectation accepting the correct measurement and naming each disagreement separately, the epsilon compared against a power of two so the check does not rest on a decimal literal round-tripping, **a silent overflow refused even when it wrapped correctly**, a missing value that became a number caught by an empty position list, the target comparison ignoring casing it cannot mean, the probe registry compared in **both** directions plus a library naming a probe the table does not describe, four-way version agreement with each register's disagreement named, **a specifier the gate cannot read reported rather than assumed satisfied**, a free-threaded wheel tag caught as a wrong artefact, and a declaration with a hole in it refused rather than defaulted |
| `test_stack_probes.py` | The measuring half: a wheel record read for its tag with the first of several taken and four unusable forms reading as absent rather than empty, the judgement registry and the callable registry compared, an unknown probe refused rather than silently passing, a distribution that is not installed reported as a finding rather than raised, and a distribution present under another import name still located; and — guarded on the project environment, because `numpy` and `pandas` arrive with the runtime lock that only `.venv` installs — **every probe run against the real libraries**, the overflow probe proven not to depend on an earlier overflow in the same process, and every probe proven deterministic across two runs, because the manifest is rendered twice and compared |
| `test_stack_gate.py` | The sequencing against synthetic trees with the measurement and the probes injected: a coherent tree passing and recording **each probe separately**, because "probes failed" answers a question nobody has; no absolute path and no clock reading in a manifest built from a real filesystem, and two runs byte-identical; an unreadable or malformed declaration, a missing runtime contract and a diverged target each still leaving a manifest; a library not installed, an installed version the lock does not pin and **a wheel built for another ABI** each refused with their own reason code; a failing probe named while its neighbours still pass, and **a probe that could not run recorded unmeasured rather than failed**; a library declared twice, a library with no probe — which fails coverage and, deliberately, *not* the registry, because the two answer different questions — and a deferral naming a delivered phase; plus the two register readers, an unusable lock or manifest reading as empty so the failure is reported per library rather than as one opaque fault, and a requirement split into a normalised name and the rest |
| `test_stack_manifest.py` | The document shape every gate under `.globin/` shares, and the ways one can disagree with itself: schema, version and phase recorded, a round trip, one sorted ASCII line with a trailing newline, the digest covering everything but itself, a changed field changing it, three things that are not manifests refused, another gate's manifest refused by name, a future version refused rather than read anyway, **a manifest edited since it was written caught with its digest left untouched**, and every reason code namespaced so one gate's cannot be mistaken for another's in the aggregate |
| `test_stack_cli.py` | One optional word: no argument and `check` accepted, five refusals including `probe` — which every sibling gate has and this one deliberately does not, because nothing it asks needs a network — an abbreviation, a flag from another command, the subcommand twice and a trailing word; a usage error printing the usage and exiting two, every exit code documented, and the usage stating it reaches nothing, so a reader can learn without opening the source that it is safe on an aeroplane |
| `test_stack_contract.py` | This repository's own declaration, which is the half that goes stale: the declared stack, the target held against the runtime contract, **every declared library also a declared runtime dependency**, the three file registers agreeing about every version, and the declared wheel tag one the lock could actually have installed; the registry agreeing in both directions and the two registries compared; every library declaring a probe and **every probe recording why it exists**, since a probe that cannot name the assumption it defends is decoration; **no probe reading the deprecated copy-on-write option**, matched on the syntax tree rather than the text because the docstrings discuss it by name and should; no question deferred to a shipped phase and all four boundaries ADR-0058 names present; the reason set compared against the gate's own source; and the handoff — `numpy` and `pandas` gone from the wheel survey, and the gate proven to reach no network by inspection rather than by opening a socket the offline guard would catch as a test failure |
| `test_stack_properties.py` | Invariants a plausible refactor breaks: binary64 accepted only when **all four** measurements match and every disagreement reported rather than the first, versions passing only when all three registers are identical — so a comparison that normalised them could not slip through — a declared version passing its bound exactly when it is not below it, **compared numerically per component** because `"2.10.0" < "2.9.0"` is true as strings and false as versions, a registry agreeing exactly when the two sets are equal, a deferral passing exactly when its phase is strictly after the frontier and inside the programme, a wheel tag never read back as an empty string because absent and unmatched mean different things, and a manifest always verifying against its own digest while two manifests share one only when they share their content |
| `test_stack_end_to_end.py` | The gate composed against a whole tree and then pulled apart one file at a time: a coherent tree passing and its manifest reading back sealed; the lock, the project manifest and the runtime contract each moved **alone** — every file individually well-formed, and what is wrong is that they no longer describe one stack; a failing run still leaving a manifest that verifies, because the evidence chain must not have a hole exactly where somebody needs to read it; the render and load paths proven to agree; the module started as a real process so the guard is exercised rather than excluded from measurement; and, guarded on the project environment, **this repository passing its own stack gate** |
| `test_diagnostics_end_to_end.py` | The subsystem the composition root assembles, against a real runtime tree rooted in `tmp_path` so no user profile is touched: the logs area created by preparing the tree, one record reaching both the console and the file under one correlation id, **nothing reaching standard output** so `--json` stays parseable, and a planted credential redacted in every destination including a nested one; hooks installed on start and restored on stop, a second start not losing the originals, and stopping twice safe; **an uncaught exception readable from the file while the subsystem is still running**, which is what proves the record was flushed rather than queued; the fault file opened and released with the subsystem; rotation staying inside the declared ceiling; the severity threshold honoured by both destinations; and the evidence record naming files rather than locating them, carrying no absolute path |
| `test_diagnostics_command_end_to_end.py` | The `diagnostics` verb driven through `main` against a runtime root redirected into `tmp_path`, so no user profile is touched: a snapshot reporting a state and exiting on it, **`--json` putting the document and nothing else on standard output**, every process reading carrying an availability so a number that was not measured is never zero, `cpu_percent` reported as unavailable rather than as `0.0`, uptime never negative, the document naming **no user and no home directory**, memory tracing off unless the verb asks for it and **switched off again afterwards** so a diagnostic never leaves the process profiling, a bundle published with a digest whose manifest describes every member except itself and whose every recorded digest recomputes from the stored bytes, no `.partial` file left behind, `--json` refused where the output is an archive, and `doctor` still answering |
| `test_stack_discipline.py` | The tripwire that keeps verification from becoming adoption, against the real source tree: no module under `src/globin` importing `numpy` or `pandas`, with the guarded set derived from the stack contract so a library added there is covered without being added twice; and the guard's own failing cases in both directions — a plain, aliased, submodule, from-import and multiple-import form all caught, **the in-function form caught** because that is the exact move somebody makes to avoid a module-level import, and an unrelated import, a package import, a relative import, a local variable and a name merely starting with a guarded one all spared |
| `test_telemetry_delivery.py` | The delivery state machine, the bounded queue and the pump between them, all with a hand-cranked clock and **no threads**: a policy refused for each of three *orderings* no per-field range check could find -- a batch above the queue capacity means nothing is ever exported while every value looks sane; backoff proved to double and then stop; the transition graph proved to have **no edge out of `STOPPED`**, which is what makes "GLOBIN never hammers a dead endpoint" a property rather than a counter; every decision proved to land on a permitted transition, the check that found two missing edges in the watchdog's own table; a retry deadline measured from **now** rather than from the attempt, so a slow attempt cannot postpone it; a full queue proved to drop the **oldest**, because during an incident the observation most worth having is the one that just happened; and -- the defect this file caught -- a temporary failure proved to **keep** its batch, since the port contracts that a batch is consumed only on delivery and a first version restored only on backpressure, losing data silently. Plus an exporter that raises proved contained, three transitions producing exactly three records, and a drain that retries once and then reports its residue rather than spending its budget waiting |
| `test_diagnostics_http_guards.py` | Every defensive guard in the surface made to **fire at least once**, with no socket anywhere: a worker that outlives the shutdown deadline recorded and left rather than waited on (joining without a bound would let one wedged request outlive the process); a worker surviving a connection that fails at the socket level, and recording only the exception's *type* because a socket error's message can carry a peer address; a peer that is not an address at all refused; the library's own error reporting proved silent on standard error; the capacity refusal proved to write its body **and then drain the request it will not serve**, which is the read that makes the 503 arrive at all rather than being discarded by a reset; a body detected however it was announced, with an unreadable `Content-Length` counting as one because that is the safe direction; and the two domain lookups that can only miss if an enum gained a member and a table did not. It exists because a guard nobody has seen fire is indistinguishable from one that cannot — and because the surface's adapter sat at 94% on a host without the optional libraries, which is below the floor even while the repository-wide average was above it |
| `test_diagnostics_http.py` | The whole behaviour of the diagnostics surface with **no socket anywhere**, through `DiagnosticsService.handle`: the bind address accepted for every loopback spelling and refused for eleven that are not — a wildcard, a LAN address, a hostname, loopback written as a decimal integer — because the set is not enumerable and the check therefore parses rather than compares; every unusable bound refused at construction, a `bool` among them; routing proved to be a table of exact strings, so traversal, encoding tricks, a trailing slash and a static-file request all reach `unknown` without being defended against; negotiation across the real Prometheus scrape header, both wildcards, a version GLOBIN does not produce, an explicit `q=0`, and fourteen malformed headers; liveness proved to answer while a health projection *raises*, which is the independence that keeps a full disk from restarting a healthy process; a truthful 503 recorded as a **success** with a 5xx class, because conflating them would make a starting process look broken; an oversized response refused rather than truncated; a switched-off route answering 404 rather than disclosing what an operator withheld; a failing projection becoming a small 500 that tells the client nothing and records only the exception's type; and every metric label proved to be a declared enum member, with a canary pushed through the path, the query and a header and found in no label, body or record |
| `test_telemetry_adapters.py` | The three things that had to touch the machine: a local exporter appending real JSON Lines to a real file, with a refused write proved **temporary** (a full disk may not be full a minute later) and a write after close proved **permanent** (nothing about it will change); `render_line` proved to refuse a `NaN`, which `allow_nan=False` turns from a number every reader parses and none can compare into a refusal; a thread proved **non-daemon** and named, started once however often it is asked, and disarmed before it is joined, with one test using a real thread because owning one is the wrapper's whole job; both provider bridges proved to record their absence rather than raise; a publisher proved to have **no way at all to bind** — the `server` field, the `listening` property and `start_loopback_listener` all asserted absent rather than false, since a field that could hold a server is a field somebody could set; the OpenMetrics encoder proved to move `_total` from the family to the sample, to emit cumulative buckets ending at `+Inf`, to emit no `# UNIT` line, and to terminate with `# EOF` even when there is nothing to report; and the exposition renderers exercised with no library involved at all, which is what makes the textfile route work on a host that has none |
| `test_library_discipline.py` | The psutil rule, stated for the two libraries Phase 026 adopted: each named in exactly one adapter and in **both directions**, so neither the rule nor its subject can quietly disappear. Plus the half that is specific to this pair -- **no second route to a listener**, because `prometheus_client.start_http_server` defaults its address to `0.0.0.0` and GLOBIN passes `127.0.0.1` as a literal, so what has to be prevented is another route appearing that does not carry that argument. The loopback address is asserted to be a literal in the module that binds, and the wildcard asserted absent outside the comment explaining why it is refused |
| `test_context_discipline.py` | The two properties `globin.application.tracing`'s ADR-0026 argument turns on, which prose cannot enforce: **exactly one module names `contextvars`**, so "reaching the variable requires being handed the scope" cannot quietly stop being true, and the span scope declares no `Logger` or `LogSink` field, since a variable holding a logger is the shortest route back to the ambient correlation ADR-0026 refused. It also asserts the variable is built **inside a function** — deliberately *not* a second import-time checker, because `test_layer_modules_execute_no_work_when_imported` already refuses every call at module scope in every layer and a second copy of that rule is the drift `SOURCE_OF_TRUTH.md` forbids; what this adds is the half that rule cannot express, that the call exists at all. Guard-the-guard rows in both directions, with the in-function import caught because that is where a convenience import actually appears |
| `test_probe_discipline.py` | The rule `numpy`'s tripwire cannot state, on the real import graph: `psutil` is declared **and imported**, so what must hold is not "nobody touches this" but "exactly one module does" — `globin.adapters.health` and no other, because the CI `quality` job has no psutil and the absence is handled in one place so every layer above can be written as if the library were present; plus the guard's own failing cases in both directions, with the **in-function form caught** because that is exactly how `system_process_probe` reaches it, and a name merely starting with `psutil`, a relative import and a local variable all spared |
| `test_process_discipline.py` | Exactly one module in the package may start a child process, it is named, and it does: no other module imports `subprocess` or `multiprocessing`, no module anywhere reaches a process through `os.system` and its neighbours, no module unbinds one of those names from `os`, and the named module still imports what the rule is about -- so deleting the adapter cannot make the first rule pass more convincingly. The `os` check matches only the *qualified* form, because a bare attribute name flagged `HostFacts.system` in seven modules that start nothing |
| `test_provisioning_cli.py` | What the provisioning verbs print and return: a healthy host told there is nothing to do, each planned action named with its mutation class, what follows it and what an interruption leaves, the destructive one marked, an action the policy forbids named rather than dropped, an outstanding claim printed first because it is what a reader most needs, an incomplete run saying the claim is still there, canonical ASCII JSON, and `bootstrap plan` run against this repository under both output modes |
| `test_provisioning_contract.py` | The provisioning surface's contract: the six bootstrap verbs and which two may mutate, every verb documented in `--help`, `verify` refused by name with its replacement named and that replacement asserted to parse, the network flag refused where it would mean nothing and accepted only as a declared policy, `CheckStatus` still four words with `ActionOutcome` disjoint from it, no twenty-sixth exit code, no action answering a `secrets.*` or `config.*` check, nothing installing a runtime, WinGet detected and never invoked, every action GLOBIN cannot perform naming the command that can, `CommandRequest` having no `shell` field at all, the manifest carrying no timestamp and no section name that trips the redactor, the child environment allowlisted rather than filtered, and the operator document covering every verb without advising anything unsafe |
| `test_provisioning_adapters.py` | The provisioning adapters, including the one real child process GLOBIN starts: a child that runs, one that fails, one that hangs and is ended as a timeout rather than an exception, one that does not exist, and one that prints past the capture ceiling and is cut. Plus the read-only runner refusing anything but a declared probe, the tool probe recording an unmeasurable probe as unmeasured rather than absent and preferring the install manager over the legacy launcher, the claim round-tripping its policy and falling back to the strictest when a marker is damaged, the executor reporting an operator's action with the command rather than attempting it, and a path outside the repository recorded as a fingerprint |
| `test_process.py` | The bounded-command value types from literals: every declared shell metacharacter refused in both the executable and the arguments, a path with a space accepted because refusing one would refuse Windows, both ends of the timeout range accepted and everything outside it refused, a boolean timeout refused because `bool` is an `int` and `True` would mean one millisecond, `CommandRequest` having no `shell` field at all, a result recording how many bytes came back rather than what they said, an unmeasured probe unable to claim presence or a version, an inventory refusing a repeated or a missing tool, and the install manager preferred over the legacy launcher |
| `test_provisioning.py` | The provisioning domain from literals: the action catalogue declared once and in dependency order, exactly one destructive action, no action constructible against a credential or configuration check, a destructive action unable to claim it is resumable, every network policy and requirement pair written out, a plan derived from a report and from nothing else, the destructive action never planned by inference, a rebuild subsuming a create, at most one non-destructive action per postcondition -- the rule that caught two actions running one command twice -- and every exit code the surface can return |
| `test_provisioning_end_to_end.py` | Provisioning composed over doubles, with the three properties types cannot assert: producing a plan starts no process and leaves the tree byte-identical, a second run over an unchanged host is a no-op by three independent measures, and a failed step stops the run, leaves its claim behind, takes no re-measurement and cannot read as ready. Each has a negative control beside it, because a fingerprint helper that could only report "unchanged" would make the first two pass for ever |
| `test_credential_discipline.py` | The same rule as its neighbour, for two Win32 libraries rather than a distribution: `advapi32` may be loaded only by `globin.adapters.secrets` and `kernel32` only by `globin.adapters.environment`, enforced on the real import graph in **both directions** — nothing else loads them, and each permitted module still does, so deleting a factory fails here rather than silently removing a capability. Matching is on the *argument* to `WinDLL`/`CDLL`/`LoadLibrary` rather than on `import ctypes`, which is legitimate anywhere in the adapters layer. A third test proves neither load happens at import, which is the property the CI story depends on — `globin.adapters` is imported by the smoke test on hosts where neither library exists. Plus the guard's own failing cases, including a computed library name recorded as **skipped rather than guessed at**, which is this check's honest limitation |
| `test_logging_discipline.py` | The tripwire that keeps ADR-0026 true against the real source tree: only the bridge adapter importing the standard library's `logging`, nothing else configuring the root logger, and a process fault hook written only by the two adapters that own one; and the checkers' own failing cases in both directions — plain, aliased, submodule, from-import and **in-function** imports all caught because that is the exact move somebody makes to avoid a module-level one, while a module merely sharing a prefix, a local of the same name and an unrelated import are spared; and, for hooks, **writing through an injected registry deliberately not flagged**, because a detector that punished the house pattern would push the next author towards touching `sys` directly |
| `test_runtime_state.py` | The runtime-state judgements from literals, so a boundary violation is described without creating one: eight segment forms refused — traversal with and without whitespace, both separators, a bare drive, an absolute path and the empty string, which reports once and stops because every later rule would fire on it too; a layout that could escape its own root unbuildable, and every bad segment reported at once; instance metadata publishing a fingerprint and never a path, and refusing a record that identifies no process; **an open lifecycle record and a closed one held to different shapes** — a stopped run that ended at no particular moment and a running one that already returned are both unrepresentable — a round trip through the published form, a future schema version refused rather than read anyway, and seven field types refused by name including **a boolean pid**, which Python would otherwise read as process one; and the four judgements, of which the one that matters is **an open previous record warning rather than refusing**, because whether an instance is running is the lock's question and only the lock's |
| `test_runtime_state_adapters.py` | The only module that touches the world, against trees in `tmp_path`: a root resolved under the platform's per-user area and **refused outright when the platform will not say where that is**, never published as a path; every area created, an area or a root existing as a file reported rather than overwritten, and **the root resolved after it exists** — the ordering bug this caught, where a junction on the way makes a root resolved before creation disagree with an area resolved after it; rendering refused for `NaN`, both infinities and an unserialisable value; and the failure injection the whole `FileOperations` seam exists for — `makedirs`, `flush`, `fsync` and `replace` each broken alone with **the previous document intact every time**, the temporary artefact cleaned up, a cleanup that also fails not replacing the fault worth reporting, an unrepresentable document never reaching the destination, and a name that could leave the area refused by all three methods; a corrupt document refused rather than treated as absent; a run releasing only its own temporary directory with the other areas surviving; and the lock taken, re-taken after release, **left behind as a file that proves nothing**, released even when the block raises, a lock file that cannot be opened reported as a broken tree rather than a busy one, and a refused acquisition named as an active instance |
| `test_lifecycle.py` | One run's lifecycle with every port a double and the clock manual, so a shutdown after a crash, a signal and a failed cleanup are all asserted without any of them happening: a completed run recorded clean, **the lock taken before a single byte is written** so a refused second coordinator overwrites nothing, the instance metadata published and then cleared, the temporary directory claimed and released, handlers installed and a fallback registered, the teardown order ADR-0059 fixes, and no absolute path in any record; an interruption, a signal and an unhandled exception each recorded with their own reason while **the original failure is what propagates**; cleanups running newest first, every one running however the ones before it went, and a failed cleanup not stopping the rest of the teardown; every teardown failure collected rather than only the first, **the lock released even when the teardown fails**, and a failure opening the run raised before the block is entered; and the `atexit` net closing an open record, leaving a clean one alone, ignoring another run's, and swallowing its own failure because the interpreter is already leaving |
| `test_diagnostics.py` | The diagnostic adapters with every process hook a dictionary, so no test ever installs a real one: records written as JSON Lines and read back, a credential-shaped field redacted before it reaches the file including one nested inside a list, the file appended to rather than truncated, rotation firing at its bound, **no more backups kept than declared**, backups shifted newest-first so none overwrites the one after it, and a count of zero discarding rather than keeping; emitting without opening refused rather than silently dropped; the three hooks installed and restored, a second install not losing the original, **an orderly exit recorded at `INFO` rather than `CRITICAL`** because an operator who sees catastrophe on every Ctrl-C stops reading it; an object whose `repr` raises and an exception whose `str` raises each still producing a report, since a half-destroyed object is exactly what an unraisable fault describes; **a reporter whose own sink is broken falling back to standard error** rather than replacing the fault with a worse one; the standard-library bridge mapping an invented level down rather than raising, carrying its originating logger as a field, and reporting its own failure through `handleError` rather than into the library that was merely logging; and a rotation policy that could not be honoured being unbuildable |
| `test_gpu_plan.py` | Every GPU judgement from literals, so the branches this host cannot produce are the ones most covered: a working host reporting all five capabilities; **the deprecated CUDA label never read as a version**, which is the trap the whole contract exists for; a host with no driver recorded `ABSENT` for presence and `UNMEASURABLE` for everything downstream — a different claim, recorded as one — and **not failing the gate**, which is what lets continuous integration run at all; a driver that refuses the query recorded `ERROR` and failing, because not knowing why is a different fact from knowing why; a driver reporting no devices being an absence rather than a fault; a short row refused rather than guessed at; a driver version that is not a version refused by shape; several devices reporting distinct capabilities sorted and de-duplicated; **a toolkit never inferred from a runtime and each asked independently**, which the target host itself demonstrates; a capability the classifier never reached recorded `UNMEASURABLE` rather than omitted; and every parser and refusal path exercised, including a phase owned by the phase that records it |
| `test_gpu_gate.py` | The GPU gate with the host substituted, so the file runs identically on a machine with a device, one without, and CI's GPU-less runner: a working host passing and its manifest carrying the observed driver and CUDA runtime; **a host with no NVIDIA device also passing**, which is the property continuous integration depends on; a toolkit recorded even with no driver, because neither is inferred from the other; a driver that errors failing; two runs producing byte-identical manifests; the manifest ending in one newline with no carriage return and carrying **no absolute path**, since it is published as a public artefact; a missing and a malformed contract each still leaving a manifest, because a gate that left no artefact is indistinguishable afterwards from one that never ran; an unreadable runtime contract reported as a diverged target rather than a crash; everything printed being ASCII; the probe **skipping the query entirely when the command is absent** so that no-driver and wrong-question do not arrive looking alike, asking for exactly the declared fields, and reporting rather than raising on an `OSError` and a timeout; and the command line refusing every unrecognised argument, printing usage rather than a traceback, reporting an unwritable artefact directory as unmeasured, and the module started as a real process so the `__main__` guard is exercised rather than excluded from measurement |
| `test_health.py` | The pure health vocabulary: a measured reading carrying its number and an absent one carrying a declared reason, **both halves refused when they disagree** — a measured reading with no value and an absent one with a value are each a state that makes every consumer wrong; a result redacting its own details on construction and refusing an oversized summary, too many details or an undeclared reason; the registry ordered, unique and ending in the aggregate; thresholds refusing a warning band of zero width; and the reduction across all-pass, warn, failure dominating everything, a **predicted** unmeasurability leaving a system healthy and an unpredicted one degrading it; plus a snapshot refusing results that are out of registry order or name an unregistered check |
| `test_support_bundle.py` | The pure bundle judgement, where path safety is testable without a filesystem and without Windows: thirteen unsafe member names each refused for a real extraction failure — traversal, a drive letter, a backslash, a reserved device stem with and without an extension, a trailing dot or space, a control character; limits refusing an impossible bundle and a per-member bound above the total; entries refusing a malformed digest or an unsafe member; and a manifest refusing entries out of order, a repeated member, or **two members differing only in case**, which a case-insensitive filesystem cannot extract |
| `test_watchdog.py` | The watchdog's pure domain, where a stall that takes thirty seconds in production takes none: a policy refusing each value out of range **and the two orderings no single range check would catch** — a stall threshold at or below the poll interval, which declares a stall on every tick, and an escalation grace below it, which expires unobserved between two ticks; a component name that could not be published and a sequence that went backwards; a snapshot refused when its beats are unordered, repeated, or **stamped later than the snapshot containing them**, which is the copy-order race turned into a domain refusal; the quietest required component chosen with ties broken by name and an advisory one never chosen; the graph asserted as absences — **no settled state reaching health, and exactly one edge into `STALLED`** — plus every decision branch from literals, including a late beat after escalation recorded and ignored, and the deadline measured from the stall rather than from the request so a slow capture cannot postpone it |
| `test_watchdog_properties.py` | The watchdog's invariants over generated input, and it **found four real defects** rather than confirming a design: `decide` raising when an episode predated its own snapshot; a disarmed episode re-arming itself to `healthy`, which would have let a tick surviving `stop()` undo the disarm that makes a surviving loop harmless; a healthy episode falling back into start-up grace; and two edges the transition table did not contain — `starting -> stalled`, reachable whenever a long grace expires over a short threshold, and `healthy -> stalled`, reachable whenever two ticks fall far enough apart to skip suspect. What it asserts: that `decide` is **total**, that every decision lands on a state the table permits, that no settled episode returns to health, that silence is never negative, and that a policy either validates or refuses with nothing half-built |
| `test_watchdog_end_to_end.py` | The watchdog wired by the composition root against a real temporary runtime tree, with the clock still injected so nothing sleeps: an incident **fsynced and renamed onto its destination** and read back, the escalated version replacing rather than joining it, **no partial file left beside it**, the real collector's frame locations carrying no drive letter or separator, and `f_locals` proven absent from the bytes an operator would open; plus the command line — the policy reported when nothing has stalled and exit zero, a recorded incident reported and exit one, `--json` carrying the document alone with the human text on standard error, and the report proven to **write nothing and start no watchdog** |
| `test_watchdog_escalation_subprocess.py` | The one claim no fake can make, made by a process that actually dies. A child interpreter arms a real watchdog with the real `ImmediateProcessExit` over a component that never beats, and the parent reads the status the operating system reports: **exit code 23**. The second test is the reason the ordering inside a confirmed stall exists — `os._exit` runs no cleanup, so the incident is asserted present on disk *after* the process that wrote it is gone. Both waits are bounded and both children are killed in a `finally`, so a broken escalation reports rather than hangs the suite |
| `test_watchdog_contract.py` | The watchdog's promises to the rest of the repository: **a stall incident proven absent from the support-bundle allowlist**, which is the entire enforcement behind ADR-0066 and whose deletion should read as deleting that decision; the settings count, the exit code and every state name in `RUNTIME_WATCHDOG.md` compared against the code that defines them; the schema name spellable under the serialization alphabet; escalation proved terminal except for standing down; and the reason and event vocabularies proved disjoint, since a shared member would make a log field ambiguous |
| `test_watchdog_coordinator.py` | The escalation machine with every port a hand-written double and the clock moved by hand, so the whole chain runs in microseconds and always in one order: arming recording the thresholds it will hold the process to and **not restarting the grace when called twice**; a heartbeat producing no record at all and only a transition producing one, because a record per beat at the default interval buries the one that mattered; suspect warned once and recovery said out loud; a confirmed stall claiming, capturing, publishing and *then* asking — asserted as an ordering, because if the ask fails the next thing is a termination that runs no cleanup; **exactly one incident however long the stall lasts**; a late beat recorded with its old and new sequence and reversing nothing; a collector that raises, a capture that half works, a publication that fails and a watchdog with nowhere to publish each leaving the escalation intact; the deadline ending the process with its own exit code and the incident republished as escalated first; escalation switched off keeping everything except the killing; and a published document carrying its schema, rendering a frame without a frame object, and redacting a credential-shaped detail |
| `test_watchdog_adapters.py` | The three things that had to live in the adapter, each tested for the property that put it there: the registry seeding a beat at registration so never-beaten needs no special case, refusing a second registration and **refusing a beat for a name nobody registered** rather than doing nothing, advancing a sequence rather than a timestamp, handing out an immutable copy, and losing none of eight hundred increments across four threads — which is the single read-modify-write the lock exists for; **a snapshot never dated before the beats it contains**, which is the copy-order race; the thread named, **not a daemon**, started once however often it is asked, arming and disarming its cycle, joined for a bounded time and reported when it will not join, running exactly one tick because its own source wakes it, and a raising tick stopping the loop once rather than flooding it; the capture writing an incident marker before the native dump, **describing the main thread but never itself** — asserted from a worker, since that is where a real capture runs — reducing every frame location to something naming no person, honouring the frame bound, and recording a failed collector while the other still runs; plus the terminator's exit injected, so no test ever reaches `os._exit` |
| `test_health_adapters.py` | The probes that read this host, with every psutil failure arranged through a hand-written double because `AccessDenied` needs another user's process and `NoSuchProcess` needs a race: a refused read becoming `denied`, a vanished or zombie process `unavailable`, a platform with **no handle count recording `unsupported` rather than zero**; the host probe collapsing duplicate anchors, reporting a failed disk query as an absence and host memory as unavailable without a reader; the thread inventory finding the main thread, a live worker and its daemon flag, **excluding it once it has finished**, and sanitising a name a dependency was free to set; the allocator tracer off by default, reporting current, peak and its own overhead when on, **never stopping a tracer it did not start**, and sanitising a location so no user profile is published; plus the real probes run once each against this interpreter |
| `test_health_collector.py` | The collector with every probe substituted, so a threshold is crossed without a full disk: every check's pass, warn, fail and unknown branch including the worst filesystem deciding, a large resident set warning and **never failing** because GLOBIN has no basis yet for saying what its own should be, a lock held elsewhere warning rather than failing, a diagnostics subsystem that was never started reading as tolerated-unknown and one that faulted as a failure; and containment — a probe that raises producing an `UNKNOWN` result rather than taking the snapshot with it, the failure **reported rather than discarded**, only the exception's type recorded so a credential in its message cannot travel, and a `KeyboardInterrupt` deliberately not contained |
| `test_support_adapters.py` | Building a bundle against a real temporary directory, because member ordering, normalised metadata, a digest that verifies and a partial file that never appears are properties of a file: members written in lexicographic order whatever order they arrive, every member stamped `1980-01-01` with fixed attributes, **the same logical inputs producing identical bytes**, a repeated member and a case-collision refused; validation catching a missing member, an undescribed one, a wrong digest, a wrong size, a missing manifest and an unreadable archive; an oversized archive refused leaving nothing behind; the budget spent so the snapshot survives the logs; and a synthetic credential corpus proven absent from the archive's raw bytes and from every extracted member |
| `test_benchmark_plan.py` | The benefit gate's pure judgement with every measurement written by hand: a contract that parses and every malformed shape refused — another schema, no workloads, a missing table, a boolean count, a missing threshold; a contract this harness cannot act on reported for an unimplemented reduction, an oversized problem, an unknown backend, a baseline carrying a threshold and **a device workload with no baseline to be divided by**; the phase floor applied asymmetrically, so a `cpu` workload may name a delivered phase and a device workload may not; and classification, where a workload with no measurement becomes an `error` rather than vanishing, because a missing row and a row saying "this failed" look identical in a summary |
| `test_benchmark_gate.py` | The benefit gate's sequencing with measurements injected, so nothing waits for a clock: a coherent tree passing and recording what it measured, **an unadopted backend exiting zero** because absence is a state, an errored workload failing, and **the derivation proven to be a function of its inputs** even though the timings are not; a missing, unparsable or wrong-host contract each still leaving a manifest; the commit read from `.git` through a reference and detached; the manifest refusing another schema, another version and a tampered document; the real measurement path exercised for all three workload families with an injected clock; and the command line refusing every unrecognised word, with the module started as a real process |
| `test_benchmark_contract.py` | This repository's own benchmark contract held against the tree: it parses, its target agrees with the runtime contract, no workload is declared twice, every backend and the reduction are words the harness implements, every device workload has a baseline and a threshold **above 1.0** so the transfer is paid for, and **no device workload is measurable yet** — each naming the phase that would change that; plus the gate registered exactly once, sitting after `gpu`, in neither `fast` nor `full`, declaring `numpy` and deliberately not `torch`, writing only inside the ignored evidence root, and every declared reason reachable from the gate |
| `test_health_properties.py` | Three invariants over generated input, each with a real failure behind it: the reduction is total, **order-independent** — so a snapshot cannot depend on which order the collector finished in — and a failure always dominates; a tolerant registry can only improve a state, never worsen it; a reading is either measured or explained and never both; and a member name that `safe_member_name` builds passes `member_problems` again, so the archive writer and the manifest cannot disagree about what is acceptable |
| `test_gpu_manifest.py` | The GPU manifest held to the shape every gate under `.globin` already uses: schema, version and phase recorded so another manifest fed to this reader is refused by name; canonical rendering — one line, sorted keys, compact separators, ASCII only — because whitespace or insertion order would change the digest; the digest covering everything except itself, which is what makes it a seal; two builds of one run producing identical bytes; and five refusals exercised individually — not JSON, not an object, another schema, **a future version refused rather than read**, and a hand-edited document whose content no longer digests to its seal |
| `test_stack_failures.py` | The stack gate's paths a healthy tree never reaches, kept apart from the ones somebody uses so the failure paths are not read as the normal ones: a commit recorded as unknown where there is no Git, no object name or a dangling ref, and read directly from a detached head; an unparsable runtime contract distinct from a missing one and from a target that merely diverges; a declaration read from a tree without one; a probe declared with nothing implementing it; **the leak gate reached by measuring a wheel tag that carries a URL with credentials in it** — the shape a real leak takes, a value from the environment reaching the evidence unnoticed — with the manifest refused rather than written; every finding shape reduced to a verdict, including **the observed section, which carries none and must reduce to passed** or every run would be unmeasured; a `WHEEL` record that cannot be read while the version still can; a module name the import machinery refuses; a probe whose library will not import; **a manifest that cannot be written reported as unmeasured rather than failed**; and two judgements a healthy stack never reaches |
| `test_runtime_state_failures.py` | The runtime-state paths a healthy machine never reaches, which fire exactly when somebody is already having a bad day and must produce a sentence rather than a traceback: a root that cannot be created reported rather than raised, an area that cannot be created named per area, and **an area resolving outside its root refused before it is created** — a branch a valid layout cannot reach today, which is exactly when a boundary check is cheap to add; a run identifier that could leave the temporary area refused where the delete happens rather than only where the name was chosen, and a temporary directory that cannot be created or removed named; a document that exists but cannot be read distinguished from absent and from corrupt; **a lock failure that is not contention not reported as contention**, since sending an operator hunting for a process that does not exist is worse than saying nothing; a probe reporting a broken tree as a sentence; a signal this platform lacks skipped rather than registered; and the `atexit` hook registered for real and immediately unregistered, because leaving it would run a test's callback long after the test that made it |
| `test_instance_lock_subprocess.py` | The property one interpreter cannot establish about itself, across real Windows processes with a file-based handshake and every wait bounded: a holder taking the lock, a second process refused with **the exit code a launcher branches on**, the holder exiting, and a third acquiring; **a process leaving through `os._exit`** — no `finally`, no `atexit`, no unlock — and the next process still acquiring, which is what makes a stale lock file harmless and deleting one both unnecessary and wrong; and two trees not contending, which is what makes these tests safe to run beside a real GLOBIN. Every child is killed and its pipes closed in a fixture, because a leaked holder would fail the *next* test rather than this one |

The architecture tests are contract-level despite reading source files, because
they assert a project invariant rather than a behaviour. They parse the syntax
tree rather than importing the modules — importing would execute them, and one
of the rules under test is that importing executes nothing.

The zero-dependency assertion deserves a note. The zero-budget rule (ADR-0003)
is easy to state and easy to erode — one convenient library at a time. Parsing
`pyproject.toml` and asserting the dependency list is empty means the first
runtime dependency cannot be added without also editing a test that says why
that list should stay empty. The policy becomes something CI notices.

## Principles

### Test invariants, not appearances

Never snapshot a whole Markdown file or a formatted report. A test that fails on
every editorial improvement teaches contributors to update expectations without
reading them, which destroys the value of every other assertion in the file.

`ROADMAP.md` illustrates the alternative. Rather than comparing it to a stored
copy, the document is written in a fixed table shape, parsed with one regular
expression, and checked against the band skeleton encoded in
`src/globin/roadmap.py`. Prose can be improved freely; structure cannot silently
break.

### Test the rule, not the restatement

`assert PROJECT_NAME == "GLOBIN"` is worth writing because it pins a value the
rest of the system depends on. A test asserting that a constant equals itself is
not. If a test cannot fail for an interesting reason, delete it.

### Keep test helpers trivial

Helpers written for tests — such as the ROADMAP parser in `tests/support.py` —
stay small and obvious. A helper complex enough to contain a bug needs tests of
its own, at which point it belongs in the package.

### Determinism is mandatory

The same source, the same inputs and the same controlled environment must give
the same result. No test may depend on wall-clock time, network availability,
execution order, the machine's environment variables, the working directory it
happened to start in, or unseeded randomness. Warnings are errors
(`filterwarnings = ["error"]`), so a deprecation surfaces when it appears rather
than when it breaks.

Consequences worth spelling out:

- Tests must not share mutable state, so running one alone must give the same
  result as running it in the middle of the suite. The isolation fixtures make
  the common cases of this enforceable rather than aspirational.
- A test that writes must write to `tmp_path`. Leaving the repository in a
  different state than it found it makes the next run depend on the last one.
- No `sleep` as synchronisation, and no assertion of the form "this took roughly
  so long". Both are timing assertions, and a shared CI runner will eventually
  break them. This is also why Hypothesis runs with its per-example deadline
  disabled.
- Never depend on set or dictionary iteration order reaching a reported result —
  `ENGINEERING_CONTRACT.md` invariant 3. `tests/property/` asserts this directly
  for the architecture review, whose output is sorted precisely so that two runs
  over one tree report identically.
- Flaky tests are diagnosed, never retried. There is no rerun plugin, and adding
  one would convert a real defect into an intermittent green build.

### Guard every checker with its failing case

A validator whose negative case is never exercised is indistinguishable from one
that cannot fail. A regex refactor is enough to make a check silently match
nothing, and nothing goes red.

So each checker in this suite has a companion test that feeds it something bad
and asserts it complains: the link checker is given a broken link, the
architecture review a synthetic violation, the import-time check a module that
does work, and the marker configuration an unregistered marker.

Where a negative case could pass because the harness itself is broken, a
positive control runs alongside it — the same fixture with only the thing under
test changed. That pairing is what caught the ineffective `--strict-markers`
configuration described above.

## Property-based testing

An example-based test asserts what its author thought of. A property test states
something that must hold across a described space of inputs, and Hypothesis
searches that space for a counter-example, then shrinks it to the smallest input
that still fails.

That distinction is not theoretical. The first run of `tests/property/` produced
two failures, both defects in the *tests*: one asserted that a module name never
contains `.py`, which is false for a module legitimately called `py`, and one
generated the source `import as`. Neither is an input a person writing examples
pictures.

### When to write one

When a real invariant exists. The recognisable shapes are an idempotent
operation, a round trip, a total function over a bounded domain, an ordering that
must not depend on input order, and a rule about a whole class of names or paths.
The invariants asserted today include `band_for_phase` being total over 1..320,
cycle detection being canonical and order-independent, and the architecture
review reporting identically however its input is ordered — a rule
`ENGINEERING_CONTRACT.md` had stated since Phase 001 with nothing checking it.

Not for every change. A property test over a two-element strategy is a slow unit
test, and requiring one everywhere would devalue the level. The Definition of
Done asks for one *when an invariant exists*, which is a judgement, not a
checkbox.

Do not restate the implementation. A property asserting that
`top_level_package` returns `module.split(".")[0]` passes forever and catches
nothing. The test to be suspicious of is one that would have to change if the
implementation were rewritten correctly.

### Profiles

Two, registered in `tests/conftest.py`:

| Profile | Used by | Behaviour |
|---|---|---|
| `dev` | `python -m tools.quality property`, and a bare `pytest` | Searches freely, keeps the example database so a past failure replays first |
| `ci` | `python -m tools.quality full`, locally and in CI | `derandomize` — the same code examines the same inputs every run — and no database |

The gate uses `ci` on every machine, not only on the build server. A gate that
searched a different input space per machine could pass where the code was
written and fail where it was reviewed, for no visible reason.

Both disable the per-example deadline, and both print a reproduction blob.

### Reproducing a failure

Hypothesis prints a `@reproduce_failure(...)` decorator with every failure. Paste
it above the test to replay that exact case while fixing it, and delete it
afterwards — it pins one input and would stop the search that found it.

```bash
python -m pytest -q -m property --hypothesis-profile=ci
```

A failure under `ci` reproduces on a rerun by construction. A failure under `dev`
may not, and the printed blob is how it is pinned. Do not fix a property failure
by narrowing the strategy until the failing input can no longer be generated
unless the input was genuinely invalid — as `import as` was, and as a module
called `py` was not.

## Testing that arrives with later phases

Some of the most important verification in this project cannot exist yet, but is
already scheduled:

- **Leakage prevention** (Phases 161-176). Leakage is uniquely dangerous because
  it *improves* results, so it looks like success until real money is committed.
  Tests must actively attempt to leak — shifting labels, fitting scalers outside
  folds — and assert that the framework refuses.
- **Point-in-time correctness** (Phase 101-102). Property tests asserting that no
  query can return data timestamped after its observation time.
- **Execution uncertainty** (Phase 086). Simulated timeouts and 5XX responses,
  asserting the system reconciles rather than assumes.
- **Risk ceilings** (Phase 242). Adversarial tests attempting to breach an
  immutable ceiling through every available path, asserting refusal.
- **Reproducibility** (Phase 158). Identical inputs and seeds must produce
  bit-identical backtest results.

## Running the suite

The full gate, which is what must pass before any commit:

```bash
powershell -ExecutionPolicy Bypass -File scripts/verify.ps1
```

The same checks, invoked directly — this is what CI runs:

```bash
python -m tools.quality full
```

While iterating:

```bash
python -m pytest -q
```

```bash
python -m tools.quality fast
```

The property level under the exploratory profile, which is the one that searches
for new counter-examples:

```bash
python -m tools.quality property
```

Tests import from the source tree directly — `pythonpath` is set in
`pyproject.toml` — so no build or install is required. The command table behind
these is described in
[`engineering/QUALITY_GATES.md`](engineering/QUALITY_GATES.md).

## Coverage

Branch coverage is measured over `globin` and `tools` and gated at a
repository-wide floor. The policy — the threshold, what is excluded and why — is
in [`engineering/QUALITY_GATES.md`](engineering/QUALITY_GATES.md), which owns it.

What belongs here is the reasoning about what the number means, because that is
easy to get backwards.

**Coverage is a floor, not a target.** Phase 004 introduced a threshold, which
reverses this document's earlier position that no threshold should exist. The
earlier reasoning was sound and remains true: high coverage of trivial code
proves nothing, and a threshold set as a *goal* reliably produces tests written
to satisfy it. What changed is the recognition that the same number used as a
*floor* catches something a goal never could — a module quietly losing its
tests, which is invisible in a green suite.

So the threshold sits deliberately below the actual figure. It is not there to
be raised, and raising it by adding tests that assert nothing would improve the
metric while making the suite worse. Judge a suite by what it would catch, not
by what it executed. A line-covered branch that has only ever been taken one way
is not tested, which is why the measurement is branch-aware rather than
line-based.
